# Interphase Maintenance Archive — Part 9

Continuation of [`interphase-maintenance-part-8.md`](interphase-maintenance-part-8.md). Part 8 is closed after STEP-497 because it reached the 48 KB rollover threshold. This volume is append-only.

## 2026-08-16 — STEP-498: Prepare successor-adjusted B2 baseline

**Status:** B2 continuation is explicitly authorized. A fresh fixed-ref target and the successor-adjusted baseline server are prepared; the baseline OAuth runtime is healthy and intentionally left running for exactly one fresh ChatGPT Web B2 trace. B2 is not yet scored, no candidate B2 target exists, and the candidate runtime is not running.

**Goal:** Execute only Phase 0 case B2 under the existing matched successor-adjusted A/B contract: baseline server `c43ec8e...` plus the exact STEP-493 six-path overlay versus candidate server `8a3d5dd3...` plus the identical overlay, each against a separate fresh target pinned to `c43ec8e...`, using Node 24.15.0, the retained OAuth profile/App, and the unchanged benchmark bootstrap/prompt.

**Files changed:** Added `docs/benchmarks/chatgpt-web-e2e/runs/2026-08-16-b2-baseline-successor-ready.json`, updated `docs/benchmarks/chatgpt-web-e2e/README.md` and `Memory.md`, and started this archive volume. The detached runner created ignored evidence under `.ai-bridge/benchmark-runs/`, and Git created the fresh disposable target worktree. No runtime source, target source, package manifest/lock, credentials, OAuth client/grant, DNS/Tunnel configuration, staging, commit, push, publication, or existing user change was modified.

**Contract and retained-server preflight:** Re-read the authoritative B2 contract from `docs/benchmarks/chatgpt-web-e2e/benchmark.json`: read-only Skill selector/loading navigation, exact fixed prompt, no extra setup, verification not required, and no prompt rewriting. Both retained successor server worktrees still exist at their exact detached base refs. Their only source statuses are the manifest's five modified runtime files plus one untracked capability-registry file; all six SHA-256 values match `successor-overlay.json`. `package.json` and `package-lock.json` match the approved source identity. Both `node_modules` junctions target `D:\Dev\codexgpt\node_modules`. The repository-frozen `%LOCALAPPDATA%\CodexPro\toolchains` Node 24.15.0 `prepare` rebuilt both worktrees, found `dist/http.js` and `dist/server.js`, and revalidated the exact overlay after each build. Candidate validation here is only retained-server preflight; no candidate B2 target or runtime was started.

**Fresh baseline target:** Created `C:\Users\Administrator\.devspace\worktrees\codexgpt-7c41931a` directly from target ref `c43ec8ecae9782598ebc9cf90d8df8cdde1035c1`. It is detached and clean; `git status --short --branch` returns only `## HEAD (no branch)`. No B2 seed or other setup was applied.

**Owned runtime:** Preflight found zero active benchmark runs and no listener on 8789/8790. The frozen successor config defaults an unset `CODEXGPT_OAUTH_WORKSPACE_CAPABILITY_MODE` to `oauth_cross_transport`; startup also set that exact process environment value before invoking the owned runner. Started only baseline-successor through `scripts/long-task-runner.mjs`, with the baseline checkout as cwd and exactly one `--allow-root` for the fresh B2 target. Adversarial review then found that this unscored start used the separately retained `%LOCALAPPDATA%\CodexGPT\toolchains` Node copy rather than the currently frozen `%LOCALAPPDATA%\CodexPro\toolchains` root. Both executables were version 24.15.0 with identical SHA-256, but toolchain-root migration remains gated. Because no B2 prompt had been sent, exact run `2026-08-16T18-33-02-060Z-step498-b2-baseline-successor-3ff129f4` was stopped by run ID; status became `stopped / stop_recorded` at `2026-08-16T18:41:16.827Z`, and both listeners were absent.

The retained server builds were rerun with `C:\Users\Administrator\AppData\Local\CodexPro\toolchains\node-v24.15.0-win-x64\node.exe`. Baseline-successor was then restarted through the owned runner with that same executable, the exact baseline cwd, and the same single B2 target authorization. The accepted exact run is `2026-08-16T18-42-03-280Z-step498-b2-baseline-successor-a96ffb31`; the superseded run is excluded from benchmark collection.

**Health evidence:** Accepted exact runner status is `running` with identity `worker_lease_active`, and its persisted child command begins with the frozen CodexPro Node executable. It is the only active benchmark run. Ports 8789 and 8790 listen only on `127.0.0.1`; local admin health returned 200; unauthenticated direct local-public health returned the expected 403 and was not misreported as success; public health returned 200 with `authMode=oauth` and `mcpAvailable=true`. `auth status` independently reports the retained OAuth profile configured and the runtime running. No other benchmark runtime or listener was active before startup.

**Benchmark evidence boundary:** Ready evidence is `docs/benchmarks/chatgpt-web-e2e/runs/2026-08-16-b2-baseline-successor-ready.json`. No B2 user message has been sent and no formal metric is assigned. The next message must be one fresh ChatGPT Web conversation containing only the fixed bootstrap rendered with target `C:\Users\Administrator\.devspace\worktrees\codexgpt-7c41931a` and the unchanged B2 prompt. Preserve the full UI/tool trace if available; otherwise unsupported wrong/redundant/total-call metrics remain `null`.

**Verification:** The repository-frozen CodexPro Node 24.15.0 `chatgpt-web-benchmark-runtime.mjs prepare` passed exact HEAD, package identity, six-entry overlay, approved dependency junction, TypeScript build, required artifact, and post-build overlay checks for both retained server worktrees. Under that same frozen Node root, `npm run test:focused -- test/chatgpt-web-benchmark-contract.test.mjs test/chatgpt-web-benchmark-runtime.test.mjs` passed 12/12 and `npm run policy:check` passed. Programmatic contract validation confirms the B2 prompt and rendered bootstrap are exact, all seven metrics remain `null`, the accepted command contains exactly one `--allow-root` equal to the fresh target, the superseded run is `stopped / stop_recorded`, and only the accepted run is active. The ready JSON parses as `status=ready_for_fresh_chatgpt_web`, `case_id=B2`; the four STEP-498 documentation/evidence files contain no trailing whitespace or secret-looking credential patterns. `git diff --check` passed with only LF-to-CRLF warnings on `Memory.md`, `docs/memory/archive/interphase-maintenance-part-7.md`, and `test/phase-6-model-sequence.test.mjs`. `Memory.md` is 119 lines / 17,669 bytes; Part 9 is 29 lines / 6,762 bytes before this paragraph update and remains below the rollover threshold. Final exact runner status is `running / worker_lease_active`, the persisted child executable is the frozen CodexPro Node path, the target remains detached/clean at the fixed ref, and all health checks retain their recorded values.

**Risks / limits:** Runtime health does not itself score B2 or prove model tool-selection efficiency. A final assistant answer without the complete UI/tool trace can support content scoring but cannot support wrong/redundant/total tool-call metrics. The process-local workspace capability still does not survive runtime restart. The intentionally dirty main checkout and retained benchmark artifacts remain untouched.

**Rollback / next action:** If baseline collection is abandoned, stop only accepted exact run `2026-08-16T18-42-03-280Z-step498-b2-baseline-successor-a96ffb31` with `scripts/long-task-runner.mjs stop --root .ai-bridge/benchmark-runs --run <exact-id>`; do not terminate guessed PIDs. After the fresh baseline answer is scored, stop and verify this exact run before creating a separate fresh candidate target or starting candidate. Do not proceed to C1.

### STEP-498 B2 continuation — baseline scored, candidate ready

**Baseline result:** The owner supplied the fresh ChatGPT Web B2 answer with workspace handle `ws_c9dd79bd67cea9423202ecc3493833bd`, explicit same-handle reuse, no default-workspace fallback, no mutation, and no test execution. The answer satisfies all B2 content criteria: it locates `load_skill` registration/normalization and handler branches in `src/server.ts`; traces exact-path and name/source selection through `resolveExactGlobalSkill`, `discoverExplicitGlobalSkills`, and `readRecord`; identifies `readGuidanceText` as the bounded file reader; distinguishes discovery/metadata record construction from the later returned body read; and names the direct user-Skill and selector/privacy tests without expanding into unrelated Phase 6 documentation.

**Baseline score / evidence boundary:** Formal evidence is `docs/benchmarks/chatgpt-web-e2e/runs/2026-08-16-b2-baseline-successor.json`. `task_success=true`, `context_fetch_calls=1`, `mutation_retries=0`, and `verification_completed=null`. The supplied paste says a complete UI/tool trace existed above its final answer, but the paste itself contains no actual tool-call rows. Therefore `wrong_tool_calls`, `redundant_tool_calls`, and `total_tool_calls` remain `null`; no count is inferred from the answer's detail or self-report. Target-source inspection independently confirmed the cited implementation symbols and direct test name.

**Baseline cleanup:** Exact accepted baseline run `2026-08-16T18-42-03-280Z-step498-b2-baseline-successor-a96ffb31` was stopped through the benchmark runner. Follow-up status is `stopped / stop_recorded`, `stoppedAt=2026-08-16T19:00:38.705Z`; 8789/8790 had zero listener residue before candidate preparation.

**Candidate preparation:** Created fresh target `C:\Users\Administrator\.devspace\worktrees\codexgpt-9e79d66d` at exact fixed ref `c43ec8ecae9782598ebc9cf90d8df8cdde1035c1`; it is detached and clean with no extra setup. Re-ran candidate-successor `prepare` under the frozen CodexPro Node 24.15.0 root: exact candidate HEAD `8a3d5dd3012c7c152fb7eea2fdb3fb91465ebc7e`, package identity, exact six-entry successor overlay, approved shared dependency junction, TypeScript build, required artifacts, and post-build overlay validation all passed.

**Candidate runtime:** Prestart found zero active benchmark runs and no 8789/8790 listeners. Started only candidate-successor with the same OAuth profile/App, explicit `oauth_cross_transport` mode, frozen CodexPro Node, and exactly one `--allow-root` for the fresh candidate target. Exact run `2026-08-16T19-01-34-915Z-step498-b2-candidate-successor-644b0a63` is `running / worker_lease_active`; it is the only active benchmark run. Both ports listen only on `127.0.0.1`; local admin health is 200, unauthenticated local-public health is the expected 403, and public health is 200 with `authMode=oauth`, `mcpAvailable=true`. Ready evidence is `docs/benchmarks/chatgpt-web-e2e/runs/2026-08-16-b2-candidate-successor-ready.json`.

**Files changed:** Added formal baseline and candidate-ready evidence; updated the benchmark README and `Memory.md`; appended this continuation record. No runtime/target source, package manifest/lock, credential, OAuth client/grant, DNS/Tunnel configuration, stage, commit, push, publication, or unrelated user change was modified.

**Verification:** Under frozen CodexPro Node 24.15.0, `npm run test:focused -- test/chatgpt-web-benchmark-contract.test.mjs test/chatgpt-web-benchmark-runtime.test.mjs` passed 12/12 and `npm run policy:check` passed. Both new JSON files parse; baseline records `task_success=true` with all unsupported efficiency metrics `null`, while candidate keeps all metrics `null`. Programmatic comparison proves the candidate prompt and rendered bootstrap exactly equal the benchmark contract. The candidate target remains detached/clean at the fixed ref; its accepted command uses the frozen Node executable and contains exactly one `--allow-root` equal to that target. Candidate remains the only active benchmark run at `running / worker_lease_active`; loopback/public health retains 200/403/200 with OAuth and MCP available. The five updated STEP-498 documentation/evidence files contain no trailing whitespace or secret-looking credential patterns. `git diff --check` passed with only LF-to-CRLF warnings on `Memory.md`, `docs/memory/archive/interphase-maintenance-part-7.md`, and `test/phase-6-model-sequence.test.mjs`. `Memory.md` is 119 lines / 17,726 bytes; Part 9 is 47 lines / 11,902 bytes before this paragraph update, below the rollover threshold.

**Risks / rollback / next action:** Baseline establishes task success only; it does not establish tool-call efficiency. Run exactly one fresh ChatGPT Web candidate B2 conversation using target `C:\Users\Administrator\.devspace\worktrees\codexgpt-9e79d66d` and the unchanged bootstrap/prompt. Preserve actual UI/tool-call rows if possible. After scoring, stop only exact candidate run `2026-08-16T19-01-34-915Z-step498-b2-candidate-successor-644b0a63`, verify listener cleanup, and write the matched pair comparison. If candidate collection is abandoned, stop that same exact run and confirm 8789/8790 listener cleanup; do not terminate guessed PIDs or delete the retained target. Do not proceed to C1.

### STEP-498 B2 closure — candidate scored and pair reviewed

**Status:** B2 is complete. Baseline-successor and candidate-successor both satisfy all three B2 success criteria. Candidate has a complete ChatGPT Web activity trace; baseline does not. Both exact runtimes are stopped with `stop_recorded`, ports 8789/8790 have no listener residue, and C1 remains unstarted and unauthorized.

**Goal:** Finish only the approved B2 matched successor-adjusted A/B case, preserve the strongest available tool evidence without filling missing baseline metrics by inference, stop the exact owned candidate runtime, and record the defensible pair conclusion before any later case.

**Candidate execution and result:** Used the in-app Browser's existing authenticated ChatGPT Web session and selected App `codexgpt-Windows-233`. In a fresh conversation, sent exactly the benchmark bootstrap rendered with candidate target `C:\Users\Administrator\.devspace\worktrees\codexgpt-9e79d66d` followed by the unchanged B2 prompt. ChatGPT returned workspace handle `ws_90f992ba502afcb5532b61a91f26d1c3`, explicitly reused it, and completed a read-only answer. The answer identifies `load_skill` registration and `normalizeLoadSkillRequest` in `src/server.ts`, `loadSkillStandardSelectorPathSource` in `src/tools/schemas/loadSkill.ts`, `resolveExactGlobalSkill` and `readRecord` in `src/guidance/skillDiscovery.ts`, the final `readGuidanceText` body read and underlying `fsp.open` / `FileHandle.read`, plus the direct tests in `test/load-skill-contract.test.mjs` and `test/phase-6-standard-projection.test.mjs`. It expressly distinguishes metadata/identity discovery from returned Skill-body reading and does not read unrelated Phase 6 documentation. Independent target inspection confirmed the cited symbols and test names.

**Candidate UI/tool trace and score:** Expanded the completed ChatGPT Web `Worked for 2m 25s` activity panel. It exposes exactly two CodexGPT child-action inputs: (1) `open_workspace` for the exact target with `include_tree=false` and `include_skills=false`; (2) one targeted `search` for `source === \"user\"` under `src`, explicitly carrying the returned workspace ID. No tree, read, semantic, `load_skill`, mutation, verification, or unrelated documentation call appears. Formal candidate evidence is `docs/benchmarks/chatgpt-web-e2e/runs/2026-08-16-b2-candidate-successor.json`: `task_success=true`, `wrong_tool_calls=0`, `redundant_tool_calls=0`, `total_tool_calls=2`, `context_fetch_calls=1`, `mutation_retries=0`, `verification_completed=null`.

**Cleanup:** Stopped exact candidate run `2026-08-16T19-01-34-915Z-step498-b2-candidate-successor-644b0a63` only through `scripts/long-task-runner.mjs`. Runner state is `stopped / stop_recorded` with `stoppedAt=2026-08-16T19:32:16.772Z`; no 8789/8790 listener remains. The accepted baseline run is also still `stopped / stop_recorded`. No process was terminated by guessed PID, and neither disposable target was deleted.

**Pair conclusion:** Formal comparison is `docs/benchmarks/chatgpt-web-e2e/runs/2026-08-16-b2-pair-comparison.json`. Both variants pass task success and workspace bootstrap. Candidate's isolated two-call trace is useful characterization evidence, but baseline's missing actual call rows make cross-variant wrong/redundant/total-call comparison impossible. Therefore the only defensible pair result is `task_success=tie_pass`, `tool_call_efficiency=not_comparable_missing_baseline_tool_trace`, and `p1_effect_for_b2=not_demonstrated`. This neither proves nor disproves a P1 efficiency improvement.

**Files changed:** Added formal candidate and pair-comparison JSON evidence; updated the benchmark README and `Memory.md`; appended this closure record. Browser use created the ChatGPT conversation but did not mutate either project target. No runtime/target source, package manifest/lock, credential, OAuth client/grant, DNS/Tunnel configuration, stage, commit, push, publication, or unrelated user change was modified.

**Verification:** Under frozen CodexPro Node 24.15.0, `npm run test:focused -- test/chatgpt-web-benchmark-contract.test.mjs test/chatgpt-web-benchmark-runtime.test.mjs` passed 12/12 and `npm run policy:check` passed. Baseline, candidate, and pair JSON all parse with the intended partial/full/mixed trace statuses; candidate records exactly two ordered calls and pair comparison keeps all unsupported cross-variant efficiency values `null`. Both target worktrees remain detached, clean, and exact at `c43ec8e...`. Exact runner list reports accepted baseline and candidate as `stopped / stop_recorded`; ports 8789/8790 have zero listeners. The five closure files contain no matching secret-looking credential patterns. `git diff --check` passed with only the pre-existing LF-to-CRLF warnings on `Memory.md`, Part 7, and the Phase 6 model-sequence test. `Memory.md` is 119 lines / 17,180 bytes and Part 9 is 67 lines / 18,053 bytes before this verification update, both below their limits. Post-result adversarial review remains the last gate before final handoff.

**Risks / rollback / next action:** The candidate's two-call result is one real Web observation, not a statistical claim; baseline efficiency remains unknown. Rollback for repository evidence is deletion of the two new B2 result files plus reversal of only this closure's README/Memory/Part 9 additions; do not alter runtime source, targets, or earlier append-only records. Next action is user review and an explicit choice between authorizing C1 or recollecting baseline B2 trace. Do not proceed automatically.

#### STEP-498 correction — preserve completed benchmark evidence

The preceding rollback sentence is incorrect and superseded by this correction. B2 is an observed, completed benchmark case: its formal candidate/pair JSON evidence must not be deleted, and this append-only Part 9 entry must not be reversed or rewritten. There is no runtime or source rollback to perform because both exact runtimes are already stopped and no product/target source changed. If any evidence fact later needs correction, preserve the original artifacts, add a dated correction or superseding evidence record, append the correction here, and update only the editable README/`Memory.md` indexes in place. C1 remains gated.

#### STEP-498 final adversarial review and verification

Three post-result reviewers independently audited scoring/evidence boundaries, runtime cleanup/A-B fairness, and documentation/memory consistency. Scoring and runtime reviews found no P0-P3 issue: the two visible candidate child actions reconcile exactly with 2 total / 0 wrong / 0 redundant / 1 context fetch; both content answers pass; baseline missing rows keep pair efficiency non-comparable; both accepted runs are `stopped / stop_recorded`; listeners are absent; targets, server refs, overlay hashes, Node path, and single-root authorization remain exact. Documentation review found the rollback defect corrected above. Its second proposed finding was rejected after direct target inspection: `test/phase-6-standard-projection.test.mjs:114-135` is in fact the configured-root global user-Skill privacy and explicit `$CODEX_DIR` loading test described by the candidate answer; the adjacent workspace-local implicit-disabled test begins at line 164.

After the correction, frozen Node 24 `npm run policy:check` passed again; programmatic B2 metric assertions passed; all three result JSON files parse; `git diff --check` passed with only the already recorded LF-to-CRLF warnings; the closure secret-looking-value scan returned no matches; 8789/8790 still have zero listeners. Final size check reports `Memory.md` 119 lines / 17,180 bytes and Part 9 71 lines / 19,513 bytes before this final append, both below limits. STEP-498 is complete; no C1 action was started.

### STEP-499 C1 seeded redaction regression pair closure

**Status / goal:** The owner expanded authorization to complete Phase 0 through I2. C1 is complete under the frozen successor-adjusted A/B contract: both variants received separate fresh targets at the fixed `c43ec8e...` ref with only the benchmark's missing-query-redaction seed, both real ChatGPT Web conversations repaired that seed, both exact runtimes are stopped with `stop_recorded`, and 8789/8790 have no listener residue. No P1 implementation or later phase was started.

**Baseline and candidate results:** Baseline target `C:\Users\Administrator\.devspace\worktrees\codexgpt-c5c3d33c` and candidate target `C:\Users\Administrator\.devspace\worktrees\codexgpt-0a61c1bf` began detached at the same fixed ref and independently reproduced 3 passing / 1 failing streaming-redaction test. The fixed C1 prompt was sent in a fresh conversation for each exact runtime. Both assistants identified the missing `QUERY_TOKEN_PATTERN` replacement in `redactSensitiveText`, restored only that line, reported the focused suite passing 4/4, and finished with no normalized source diff. Independent frozen CodexPro Node 24.15.0 verification confirmed 4/4, `git diff --check` without whitespace errors, and `git hash-object src/redact.ts` equal to `HEAD:src/redact.ts` for both targets. Each Web edit changed the working-file line-ending representation and temporarily left a stat-only `.M`; an index refresh with `git add -- src/redact.ts` produced no staged or working blob diff and returned each target clean.

**Tool evidence and scoring:** Both expanded ChatGPT Web panels are incomplete. They expose the same wrong initial DevSpace workspace attempt, the successful CodexGPT `open_workspace`, initial focused test, diff inspection, and targeted read after an exact-edit miss; candidate additionally exposes `git diff --check`. They do not expose the mutation calls, reported final focused-test call, or final diff call as complete child rows. Therefore neither visible partial sequence is converted into an exact efficiency denominator: both formal evidence files retain `wrong_tool_calls=null`, `redundant_tool_calls=null`, and `total_tool_calls=null`. The visible narratives support `mutation_retries=1`; exact workspace bootstrap supports `context_fetch_calls=1`; independent tests support `verification_completed=true` and `task_success=true`. Pair conclusion is `task_success=tie_pass`, `verification_completion=tie_pass`, `tool_call_efficiency=not_comparable_incomplete_tool_traces`, and `p1_effect_for_c1=not_demonstrated`.

**Runtime / evidence:** Baseline exact run `2026-08-16T19-43-40-982Z-step499-c1-baseline-successor-31453f84` stopped at `2026-08-16T19:52:53.302Z`; candidate exact run `2026-08-16T19-56-30-920Z-step499-c1-candidate-successor-f82355dd` stopped at `2026-08-16T20:02:17.181Z`. Both used the frozen CodexPro Node path, one exact target authorization, the same OAuth profile/App, exact successor overlay, and loopback-only listeners. Formal evidence is `docs/benchmarks/chatgpt-web-e2e/runs/2026-08-16-c1-baseline-successor.json`, `2026-08-16-c1-candidate-successor.json`, and `2026-08-16-c1-pair-comparison.json`.

**Files changed / verification:** Added the three C1 result JSON files, updated the editable benchmark README and `Memory.md`, and appended this archive entry. No product source, retained server ref, credential, OAuth client/grant, DNS/Tunnel setting, commit, push, or publication changed. All C1 JSON parses; the focused target tests and cleanup checks above passed. Repository contract/runtime tests, policy, final diff/secret scan, and adversarial review follow as the STEP-499 closure gate.

**Risks / rollback / next action:** One observation per variant is characterization, not statistical proof; partial panels prevent efficiency comparison. There is no runtime or product-source rollback: runs are already stopped and targets are clean. If evidence needs correction, preserve the JSON and append a correction or superseding record; never rewrite this append-only entry. Next authorized action is C2 baseline preparation under the same fresh-target/exact-stop protocol; do not skip ahead or enter another phase.

#### STEP-499 final gate and partial-trace schema correction

Post-result adversarial review found no runtime, fairness, scoring, target-state, or success-criteria defect. It identified one evidence-schema ambiguity: the variant files correctly retained efficiency metrics as `null`, but simultaneously marked `tool_trace_provided=false` while storing a continuous-looking subset in `tool_calls`. That representation is now corrected in the editable JSON evidence to `tool_trace_provided=true`, `tool_trace_complete=false`, and `observed_partial_tool_calls` with `visible_order`; the pair records the same partial-not-absent distinction. This does not change any metric or conclusion, and the original closure facts above remain intact.

The pending closure gates referenced above have now run. Frozen CodexPro Node 24.15.0 `npm run test:focused -- test/chatgpt-web-benchmark-contract.test.mjs test/chatgpt-web-benchmark-runtime.test.mjs` passed 12/12, and `npm run policy:check` passed. All three C1 JSON files parse after the schema correction. `git diff --check` passed with only the pre-existing LF-to-CRLF warnings on `Memory.md`, Part 7, and `test/phase-6-model-sequence.test.mjs`; the result-file and documentation secret-looking-value scan found no credential value. `Memory.md` is 120 lines / 17,726 bytes and Part 9 was 25,261 bytes before this correction, both below limits. Three independent reviewers reported no P0/P1/P3 finding; the evidence ambiguity was the only actionable P2 and is closed here. STEP-499 is fully closed, and C2 remains the next authorized case.

### STEP-500 C2 seeded OutputRing truncation pair closure

**Status / goal:** C2 is complete under the same frozen successor-adjusted A/B contract. Separate baseline and candidate targets at fixed `c43ec8e...` received only the benchmark seed `truncated: false`, each reproduced the expected 4-pass/1-fail state, and each real fresh ChatGPT Web task restored the physical eviction signal without changing tests or unrelated process code. Both exact runtimes are stopped with `stop_recorded`; 8789/8790 have no listener residue.

**Results and verification:** Baseline target `C:\Users\Administrator\.devspace\worktrees\codexgpt-e3d1c2b0` and candidate target `C:\Users\Administrator\.devspace\worktrees\codexgpt-f4c2d9a1` both restored `truncated: requested < earliest`. Each answer correctly explained that eviction advances the earliest retained sequence and clamps an older requested cursor forward, so the caller must receive a data-loss signal. Frozen CodexPro Node 24.15.0 independently passed `test/process-output-ring.test.mjs` 5/5, `git diff --check`, and normalized `src/process/outputRing.ts` blob equality with HEAD for both targets. Line-ending-only stat entries were refreshed without introducing a staged or working blob diff; both targets are clean.

**Evidence boundary:** Both expanded activity panels are partial. Baseline exposes the wrong DevSpace bootstrap, correct CodexGPT workspace open, relevant-file read, lexical search, exact edit, and final diff; candidate exposes the wrong bootstrap, correct open, initial/final diff, and search. At least the reported focused-test action is absent from both visible child-row sets, and candidate also omits the mutation row. Therefore both files use `tool_trace_complete=false`, store only `observed_partial_tool_calls`, and retain wrong/redundant/total metrics as `null`. Both score `task_success=true`, `context_fetch_calls=1`, `mutation_retries=0`, and `verification_completed=true`; pair conclusion is a task/verification tie with `tool_call_efficiency=not_comparable_incomplete_tool_traces` and `p1_effect_for_c2=not_demonstrated`.

**Runtime / files / rollback:** Baseline run `2026-08-16T20-09-12-796Z-step500-c2-baseline-successor-e5d153f7` stopped at `2026-08-16T20:12:36.011Z`; candidate run `2026-08-16T20-13-54-629Z-step500-c2-candidate-successor-5147b9ad` stopped at `2026-08-16T20:16:58.253Z`. Added the three C2 result JSON files, updated README and `Memory.md`, and appended this record. No product source, retained server ref, credential, OAuth/DNS/Tunnel state, commit, push, or publication changed. There is no runtime/source rollback; preserve evidence and append any future correction. Next authorized action is D1 baseline preparation, not another implementation phase.

#### STEP-500 scoring correction and final gate

Adversarial scoring review found one P2: the partial panels did not prove zero mutation retries, especially because candidate omitted the mutation row. Both variant and pair files now keep `mutation_retries=null`; README and this append-only record are corrected without changing task success, verification, or efficiency conclusions. Runtime/fairness and documentation reviews found no other blocking defect. Frozen Node 24 contract/runtime tests passed 12/12, `npm run policy:check` passed, all C2 JSON parses, and `git diff --check` passed with only the previously recorded line-ending warnings. STEP-500 is fully closed; D1 remains next.

Documentation review additionally found the pair's machine-readable controls omitted the already enforced overlay-manifest path; `successor_overlay_manifest=docs/benchmarks/chatgpt-web-e2e/successor-overlay.json` is now present. Final programmatic assertions confirm both mutation-retry metrics and the pair comparison are `null`, both traces are explicitly incomplete, and the overlay-manifest control is exact. The final credential-value scan found no value match; its only lexical hit is `Memory.md`'s standing prohibition on private keys. This append supersedes the earlier C2 paragraph's unsupported `mutation_retries=0` statement; the archive history is intentionally preserved rather than rewritten.

### STEP-501 D1 optional OutputRing offset pair closure

**Status / goal:** D1 is complete under the fixed successor-adjusted A/B protocol. Separate clean fixed-ref targets received the exact prompt to add a compatible optional `latestSequence` offset, directly test it, run focused verification, and review the final diff. Both exact runtimes are `stopped / stop_recorded`; 8789/8790 have no listener residue.

**Results:** Baseline target `C:\Users\Administrator\.devspace\worktrees\codexgpt-d1b45a7c` and candidate target `C:\Users\Administrator\.devspace\worktrees\codexgpt-d1c82f6e` both changed only `src/process/outputRing.ts` plus a new direct `test/process-output-ring-latest-sequence.test.mjs`. Each implemented `latestSequence(offset = 0)` returning `this.#next + offset`, preserving no-argument callers, and tested both default `3` and offset `2 -> 5`. The existing output-ring test contains secret-pattern fixtures and rejected modification; both assistants respected that boundary and isolated the new test. Frozen CodexPro Node 24.15.0 independently passed the new test plus `test/process-output-ring.test.mjs` at 6/6 and `git diff --check`; both diffs contain no unrelated file.

**Evidence boundary / pair:** Both expanded panels are partial. Baseline exposes open, initial diff, successful write/edit, and a final test read; candidate additionally exposes final focused test and diff. Both narratives record earlier failed exact edits and secret-protection rejection, while their child rows omit those attempts and other material calls. Therefore wrong/redundant/total and mutation-retry metrics stay `null`; both score task success, verification, and one context fetch. Pair conclusion is `tie_pass` for task/verification, `not_comparable_incomplete_tool_traces` for efficiency, and `p1_effect_for_d1=not_demonstrated`.

**Runtime / files / rollback / next:** Baseline run `2026-08-16T20-22-16-651Z-step501-d1-baseline-successor-f2e6bfcc` stopped at `2026-08-16T20:25:52.248Z`; candidate run `2026-08-16T20-26-56-924Z-step501-d1-candidate-successor-5b0f4e5f` stopped at `2026-08-16T20:31:29.084Z`. Added three D1 JSON files, updated README/`Memory.md`, and appended this record. No retained server/product source, credential, OAuth/DNS/Tunnel state, commit, push, or publication changed. Preserve evidence and append corrections; next authorized case is D2, not another implementation phase.

#### STEP-501 final-answer evidence clarification

Scoring review questioned whether baseline ChatGPT itself reviewed the final diff because the expanded child rows omit the final diff call. The retained final answer does explicitly state that its final workspace-change check found only `M src/process/outputRing.ts` and `?? test/process-output-ring-latest-sequence.test.mjs`, followed by no stage/commit/push. The baseline evidence summary now records that exact criterion-level fact. Independent verification remains corroboration, not a substitute for the assistant's review. No score changes: baseline satisfies the final-diff criterion and the pair remains a task/verification tie.

#### STEP-501 final closure gate

Frozen CodexPro Node 24.15.0 benchmark contract/runtime tests passed 12/12 and `npm run policy:check` passed. All three D1 JSON files parse; final assertions keep both traces partial-not-absent, all unsupported efficiency/retry metrics `null`, and the overlay-manifest control exact. Repository `git diff --check` passed with only the established line-ending warnings outside D1 evidence. The credential-value scan found no value match; its only lexical match is the standing memory prohibition. Three post-result reviews found no runtime/fairness defect; the baseline final-answer evidence clarification above closes the only scoring question. Pair controls now explicitly record `tool_trace_provided=true` alongside `tool_trace_complete=false` for both variants. STEP-501 is fully closed; D2 is next.

### STEP-502 D2 search-description pair closure

**Status / result:** D2 is complete as a matched failed observation, not an operator-finished feature. Separate clean fixed-ref targets received the exact description-only prompt. Both real ChatGPT Web runs located the production description in `src/server.ts`, added a direct exact contract in `test/search-contract.test.mjs`, and reached the intended 15-pass/1-fail RED state. Both then failed to modify the production file: `edit` returned `SECRET_CONTENT_BLOCKED`, while subsequent bounded `apply_patch` attempts failed patch checks. Neither assistant bypassed the security boundary. Final diff review in both cases found only the test modification, production remained unchanged, and independent frozen-Node verification reproduced 15/16.

**Scoring / evidence boundary:** Both score `task_success=false`, `verification_completed=false`, `context_fetch_calls=1`, and satisfy workspace bootstrap. Their expanded panels expose only subsets of search/read/test-mutation/patch/diff actions; baseline omits the source failures and test call, candidate omits the test mutation, test call, first blocked edit, and some patch attempts. Therefore wrong/redundant/total and mutation-retry metrics remain `null`. Pair conclusion is `task_success=tie_fail`, `verification_completion=tie_fail`, `tool_call_efficiency=not_comparable_incomplete_tool_traces`, and `p1_effect_for_d2=not_demonstrated`. Candidate's unrelated activity heading/raw API card is retained only as qualitative evidence, not scored without a complete denominator.

**Runtime / files / rollback / next:** Baseline run `2026-08-16T20-35-44-484Z-step502-d2-baseline-successor-341af885` stopped at `2026-08-16T20:40:14.547Z`; candidate run `2026-08-16T20-41-13-443Z-step502-d2-candidate-successor-57b27a1e` stopped at `2026-08-16T20:46:34.980Z`. Added three D2 JSON files, updated README/`Memory.md`, and appended this record. No retained server/product source, credential, OAuth/DNS/Tunnel state, commit, push, or publication changed. Preserve evidence and append corrections. Next authorized case is E1, not another implementation phase.

#### STEP-502 verification-completion correction and closure gate

Scoring review corrected the distinction between verification completion and verification success. Both assistants actually ran the required focused test and inspected the final diff, producing explicit 15/16 RED evidence; therefore `verification_completed=true` for both and the pair is `verification_completion=tie_pass`, while `task_success=false` remains unchanged because production was not fixed and tests did not pass. This correction updates editable evidence/indices and supersedes only the earlier interpretation, not the observed facts.

Frozen CodexPro Node 24.15.0 benchmark contract/runtime tests passed 12/12 and `npm run policy:check` passed. All three D2 JSON files parse; repository `git diff --check` passed with only established line-ending warnings. Final assertions and credential-value scan follow before rollover review. STEP-502 remains the current closure; E1 is next after adversarial findings are resolved.

#### STEP-502 final scoring assertions and review closure

Final machine assertions confirm both variants retain `task_success=false` and `verification_completed=true`, the pair records `task_success=tie_fail` plus `verification_completion=tie_pass`, unsupported efficiency/retry metrics remain `null`, and the exact overlay manifest remains controlled. The credential-value scan found no value match, `git diff --check` remains clean apart from established line-ending warnings, both exact runs remain `stopped / stop_recorded`, and 8789/8790 have zero listeners. Post-result scoring, runtime/fairness, and documentation reviews agree that a completed RED verification is distinct from task success and found no remaining blocking defect. `Memory.md` was compacted below its practical size limit without removing archived evidence. STEP-502 is fully closed; E1 is the next authorized case.

### STEP-503 E1 OutputRing public-symbol rename pair

**Status / goal:** E1 completed its matched successor-adjusted baseline/candidate run and awaits only post-result review. Separate clean detached targets at fixed `c43ec8e...` received the exact prompt to rename `OutputRing.latestSequence` to `currentSequence`, update all references/direct tests without a dual API, run focused tests, and finish with an old-symbol search plus diff review.

**Results / verification:** Baseline target `C:\Users\Administrator\.devspace\worktrees\codexgpt-694603a2` and candidate target `C:\Users\Administrator\.devspace\worktrees\codexgpt-db3c660a` each changed only `src/process/outputRing.ts` plus a new `test/process-output-ring-sequence.test.mjs`. Both expose only `currentSequence()`, add direct initial/post-append assertions, and retain no current `latestSequence` match. Frozen CodexPro Node 24.15.0 independently passed the new test with the existing output-ring suite at 6/6 for both targets; `git diff --check` passed.

**Evidence / scoring boundary:** Both score `task_success=true`, `verification_completed=true`, `context_fetch_calls=1`, and `mutation_retries=2` because the visible activity explicitly records two failed edits for the same intended direct-test mutation before the successful new-file write. Baseline's cumulative tool modal pairs some semantic outputs with shifted low-level inputs; candidate explicitly says an earlier call is hidden and omits focused-test/search/diff rows. Therefore both retain wrong/redundant/total as `null`; the pair is a task/verification tie, `tool_call_efficiency=not_comparable_incomplete_tool_traces`, and `p1_effect_for_e1=not_demonstrated`.

**Runtime / files / rollback / next:** Baseline run `2026-08-16T20-55-40-590Z-step503-e1-baseline-successor-9ce07cb4` stopped at `2026-08-16T21:04:20.653Z`; candidate run `2026-08-16T21-05-37-923Z-step503-e1-candidate-successor-11488dd0` stopped at `2026-08-16T21:10:19.278Z`. Added three E1 JSON files, updated benchmark README and `Memory.md`, and appended this record. No retained server/product source, credential, OAuth/DNS/Tunnel state, commit, push, or publication changed. Preserve evidence and append future corrections; after review, E2 is next and no later implementation phase is authorized.

#### STEP-503 final evidence boundary and closure gate

One documentation reviewer questioned whether an incomplete overall trace could support exact `mutation_retries=2`. The ordered low-level modal closes that narrower denominator independently on both variants: calls 6/7/8 are the contiguous initial test edit, retry edit, and successful retry write; the next numbered call is the distinct source rename. Baseline's later test edit is cleanup after successful creation, and candidate's hidden later action belongs to verification/review. Both evidence files now expose `mutation_attempt_trace_complete=true` and the numbered basis; overall wrong/redundant/total remain `null` because unrelated semantic rows are still hidden or shifted. Two scoring/runtime reviewers independently confirmed this boundary and found no P0-P3 defect.

Frozen CodexPro Node 24.15.0 benchmark contract/runtime tests passed 12/12 and `npm run policy:check` passed. All three E1 JSON files parse; final assertions confirm the task/verification tie, exact two-retry subtrace, incomplete overall traces, controlled overlay manifest, and stopped cleanup. Repository `git diff --check` passed with only established line-ending warnings; the E1 credential-value scan found no match. `Memory.md` remains below 18 KiB and Part 9 remains below 48 KB. STEP-503 is fully closed; E2 is next.

### STEP-504 E2 internal redaction-helper rename pair

**Status / result:** E2 completed matched successor-adjusted baseline/candidate execution and awaits post-result review. Separate clean fixed-ref targets `codexgpt-830a59b3` and `codexgpt-3e72b458` each changed only `src/redact.ts`: one internal helper definition and six same-file call sites were renamed to `isRedactionPlaceholder`; helper logic and redaction policy were unchanged. Frozen CodexPro Node 24.15.0 independently passed `test/streaming-redaction.test.mjs` 4/4 on both, found zero old-name hits, and passed `git diff --check`.

**Scoring / evidence:** Both variants score task and verification success with one context fetch. The expanded activity provides criterion-level result evidence but not a stable complete semantic child-action trace, so wrong/redundant/total/retries remain `null`; the pair is a task/verification tie, efficiency not comparable, and `p1_effect_for_e2=not_demonstrated`.

**Runtime / files / rollback / next:** Baseline run `2026-08-16T21-16-49-589Z-step504-e2-baseline-successor-63174700` stopped at `2026-08-16T21:20:04.254Z`; candidate run `2026-08-16T21-20-40-575Z-step504-e2-candidate-successor-2eb317d6` stopped at `2026-08-16T21:23:22.095Z`; listeners are zero. Added three E2 JSON files, updated README/`Memory.md`, and appended this record. No retained server/product source, credentials, OAuth/DNS/Tunnel state, commit, push, or publication changed. Preserve evidence and append corrections. After review, F1 is next; no later phase is authorized.

#### STEP-504 final closure gate

Three post-result reviews found no code, scoring, fairness, runtime, or documentation defect after the pending-state correction. Frozen CodexPro Node 24.15.0 benchmark contract/runtime tests passed 12/12 and `npm run policy:check` passed. All three E2 JSON files parse; final assertions confirm both task/verification passes, incomplete traces with unsupported metrics `null`, exact overlay controls, exact stopped-run linkage, target old-name zero/new-name seven, identical identifier-only diffs, and zero listeners. Repository `git diff --check` passed with only established line-ending warnings; the credential-value scan found no match. STEP-504 is fully closed; F1 is next. Part 9 remains active only while below its 48 KB threshold.

### STEP-505 F1 seeded secret-redaction review pair

**Status / goal:** F1 completed matched successor-adjusted baseline/candidate Web execution and awaits post-result review. Separate fixed-ref targets received the identical one-line uncommitted seed removing `QUERY_TOKEN_PATTERN` replacement from `redactSensitiveText`; the fixed read-only prompt requested risk-first review without revealing the faulty line.

**Results / scoring:** Both assistants identified the query-token credential-disclosure regression, used the current change as primary evidence, reproduced the direct streaming-redaction test at 3/4 with `secret-code` exposed, described affected output boundaries, recommended focused verification, and made no target mutation. Both score task success, one context fetch, and read-only verification `null`. Candidate exposes a complete six-action CodexGPT trace with zero wrong/redundant calls; baseline's cumulative modal pairs later search inputs with bash/show_changes outputs, so its efficiency/retry fields and pair efficiency remain `null`; `p1_effect_for_f1=not_demonstrated`.

**Runtime / files / rollback / next:** Baseline target `codexgpt-44680c4b` and candidate target `codexgpt-b34835cc` retain only the identical seed and pass diff check. Runs `2026-08-16T21-34-30-281Z-step505-f1-baseline-successor-c6111bf9` and `2026-08-16T21-39-25-460Z-step505-f1-candidate-successor-94790770` are exact stopped/stop-recorded with zero listeners. Added three F1 JSON files and updated README/`Memory.md`; no retained server/product source, credentials, OAuth/DNS/Tunnel state, commit, push, or publication changed. Preserve evidence and append corrections rather than deleting it. After review, F2 is next; no later implementation phase is authorized.

#### STEP-505 final closure gate and Part 9 seal

Three post-result reviews found no P0–P3 defect. Frozen CodexPro Node 24.15.0 benchmark contract/runtime tests passed 12/12 and policy passed; all F1 JSON/assertions, exact refs/overlay/seed/run linkage, target diffs, stopped cleanup, repository diff check, and credential-value scan passed. Candidate's complete six-action trace and baseline's incomplete mispaired trace support the recorded task tie and no efficiency conclusion. STEP-505 is fully closed. Part 9 is sealed here before the next record would exceed 48 KB; F2 begins Part 10. Preserve this volume append-only and add any future correction rather than rewriting history.
