# Interphase Maintenance Archive — Part 13

This append-only continuation begins with STEP-538 after Part 12 crossed the 48 KiB continuation threshold. Use `Memory.md` for current state and the archive index for earlier volumes.

## 2026-08-31 — STEP-538: P4 owner-bound change verification and review workflow

**Status:** completed locally under the owner's explicit instruction to execute P4 through completion. No App refresh, Web run, deployment, publication, staging, commit, push, credential, network, service, or other external-state action was authorized or performed.

**Goal:** turn a committed mutation into one explicit `apply -> verify -> review` workflow that tells the model what changed, exposes only current confirmed project checks, executes nothing automatically, and requires inspection of the matching whole-workspace diff before the change can be considered ready.

**Files changed:** `src/workflows/changeWorkflow.ts`; `src/tools/schemas/changeWorkflow.ts`; V5-compatible extensions in `src/tools/schemas/{write,edit,applyPatch,movePaths,undoChangeSet,showChanges,codexgpt}.ts`; `src/codexgptSupertool.ts`; `src/server.ts`; `src/tools/phase3dServer.ts`; `fixtures/ts-imports/change-workflow-contract-imports.ts`; `test/change-workflow-{service,contract}.test.mjs`; `test/phase-7-rename-apply.test.mjs`; `test/phase-7-v5-runtime-inheritance.test.mjs`; `scripts/test-execution-profile-manifest.mjs`; the paired P4 spec/plan; `CHATGPT_PROMPT.md`; both READMEs; `design.md`; `CHANGELOG.md`; the master plan; `Memory.md`; and the archive index/continuation. Earlier STEP-516–537 working-tree changes were preserved.

**Implementation:** added one server-scoped `ChangeWorkflowService` keyed by exact workspace and authenticated change-set identity, with current owner binding, bounded changed paths, confirmed P2 recommendations, terminal verification state, diff-review state, and no stored mutation contents, environment, secret, or unbounded output. Successful committed V5 `write`, `edit`, `apply_patch`, `move_paths`, and non-preview `undo_change_set` results gain `data.workflow`; failures and previews do not. Added `verify_change` only as a V5 wrapper-owned composite action, not a direct tool: callers choose a bounded check category but cannot supply command text, executable, environment, shell mode, or process authority. The server re-detects current `confidence=confirmed` project commands, compiles a fixed PowerShell child request, and invokes the already registered `run_command` handler, preserving full-mode projection, Policy, local approval, audit, redaction, timeout, and cancellation. Policy denials pass through without executing the child. V5 `show_changes(change_set_id=...)` completes review only for an owner-matched, unscoped, included, marked, non-checkpoint diff whose current changed paths include every workflow path. Terminal failed checks make the workflow complete after review but never ready. Invalid explicit review linkage preserves the valid diff and returns a bounded user-visible warning.

**Compatibility and authority:** no direct tool was added or removed; V1/V2/V3/V4/V5 remain exactly `28/31/39/51/52`. V1–V4 descriptors and payloads remain exact. Standard/minimal modes receive no execution authority; mutation success never starts a command. Existing atomic transaction, change-set, undo, OAuth scope, Policy, approval, audit, process, redaction, Git, and rollback contracts remain the enforcement boundary. Workflow state is deliberately server-local and is not claimed to survive restart.

**TDD and focused verification:** the first focused RED run failed both new files with the expected missing workflow schema/service modules. After implementation, the service/contract suite passed 8/8; the final registered apply/verify/review integration proves zero automatic calls, confirmed-command compilation, Policy-denial passthrough, real registered child execution, failed-link feedback, path-complete whole-workspace review, and readiness distinction. The final managed Node 20.20.2/24.15.0 focused matrix passed 23/23 on each across the P4 service/contract, registered semantic mutation, and `show_changes` compatibility. A broader local closure set passed 48/48 across P4, supertool, mutation inventory, execution-profile inventory, package, and documentation contracts. Both managed builds passed before the final documentation-only evidence append; `npm pack --dry-run --json` passed with 694 files and included `dist/workflows/changeWorkflow.js` plus the new schema and sourcemaps.

**Ordinary and smoke:** final detached ordinary run `2026-08-31T07-06-23-444Z-p4-final-ordinary-r2-85e1ea7b` completed exit 0 with complete 484,383-byte stdout, empty stderr, and cleaned temporary state. On each of Node 20.20.2 and Node 24.15.0, fast passed 1243/1244 with one platform skip, safe passed 299/300 with one platform skip, and isolated passed 68/68. Final detached smoke run `2026-08-31T11-59-13-830Z-p4-final-smoke-9e2a43da` completed exit 0 with complete 714-byte stdout, empty stderr, and cleaned temporary state; analysis, analysis CLI, base, HTTP, Pro CLI, doctor, settings, and execute/watch/loop handoff smoke passed on both Node majors.

**Failed attempts retained:** the initial focused RED failures were expected and established the TDD boundary. The first end-to-end expectation used `npm test`, while the existing P2 detector correctly returned `npm run test`; the test was corrected to the actual confirmed command. A first review-path hardening test exposed that `changedStatusLines` stripped the leading porcelain status column; the bounded fix preserves leading status spaces and the complete `show_changes` suite passed. The first ordinary run `2026-08-31T06-52-25-646Z-p4-change-workflow-ordinary-r1-25efcb05` is superseded: it failed only the source-stability acceptance because source edits intentionally continued during the run, and its Phase 7 provider correctly reported `Semantic source changed during analysis.` The frozen-tree r2 run above is authoritative.

**Final repository gates:** `npm run policy:check`, `git diff --check`, added-line high-confidence credential scan, direct-tool count/composite-only scan, mutation inventory, execution-profile inventory, package-content check, no-staged-file check, memory/archive size check, and final documentation/link checks passed. Informational LF-to-CRLF warnings remain unchanged. The final documentation-only evidence append was followed by a fresh dual-Node build and focused documentation/package/Policy checks.

**Risks and limitations:** workflow verification executes trusted repository scripts with the current user's ambient `full_access` authority and is not a sandbox; it is explicit and protected by existing gates but can still change host state. Ephemeral workflow verification/review state does not survive server restart. A project with no current confirmed command receives an unavailable state rather than an invented command. A completed workflow with failed checks remains not ready. No fresh UI/tool trace exists, so `wrong_tool_calls`, `redundant_tool_calls`, and `total_tool_calls` remain unscored and no Web efficiency reduction is claimed.

**Rollback:** remove the two P4 modules and import barrel, remove V5 workflow fields and the wrapper-owned composite action, restore prior mutation/show-change output variants and porcelain-line normalization, remove P4 tests/profile entries and user guidance, then append an archive correction rather than rewriting this record. Existing files/change sets remain governed by the unchanged transaction/undo systems. No App, deployment, profile, credential, branch, commit, remote, or external service requires rollback.

**Next approved action:** none beyond P4. App refresh, fresh Web evidence, runtime deployment, publication, staging/commit/push, credential/network/service changes, external Providers, deferred phases, and the `NGROK_DOMAIN` warning/retention decision remain separately gated.

## 2026-08-31 — STEP-539: P4 exact final-review checklist and frozen closure evidence

**Status:** completed locally. This append-only correction closes the final acceptance detail added after STEP-538 without rewriting that earlier record. No App refresh, Web run, deployment, publication, staging, commit, push, credential, network, service, or other external-state action was performed.

**Goal:** make the roadmap's final review obligations machine-readable and user-visible, then prove the resulting frozen source and shipped guidance on both retained Node majors.

**Files changed:** `src/tools/schemas/changeWorkflow.ts`; `src/workflows/changeWorkflow.ts`; `test/change-workflow-{service,contract}.test.mjs`; `CHATGPT_PROMPT.md`; both READMEs; `design.md`; `CHANGELOG.md`; the paired P4 spec/plan; the master plan; `Memory.md`; and this archive. All pre-existing STEP-516–538 working-tree changes remain preserved.

**Implementation and user impact:** every workflow review projection now carries the exact ordered checklist `unexpected_files`, `formatting`, `generated_artifacts`, `dependency_changes`, and `accidental_deletion`. The checklist is a required inspection contract over the complete current workspace diff, not an automatic heuristic verdict. Unit and contract tests freeze the tuple and the verification next-action text names the same five categories. Shipped ChatGPT, English, Chinese, design, and changelog guidance now agree, so users see one explicit final-review definition instead of an ambiguous “inspect the diff” instruction.

**Focused and package verification:** after the checklist change, the local P4/service/contract/rename/show-changes/supertool closure set passed 36/36. The managed Node 20.20.2 and Node 24.15.0 P4 matrix passed 23/23 on each. `npm pack --dry-run --json` rebuilt successfully and reported 694 package entries including the workflow/schema JavaScript and sourcemaps.

**Authoritative ordinary evidence:** detached run `2026-08-31T12-19-26-448Z-p4-final-ordinary-r3-1146656d` completed exit 0 from the checklist-bearing tree with complete 484,456-byte stdout, empty stderr, and cleaned temporary state. On both Node 20.20.2 and Node 24.15.0, fast passed 1243/1244 with one platform skip, safe passed 299/300 with one platform skip, and isolated passed 68/68. The earlier r2 remains valid pre-checklist P4 core evidence but r3 is authoritative for the final tree.

**Authoritative smoke evidence:** detached run `2026-08-31T12-42-59-759Z-p4-final-smoke-r2-b896f209` completed exit 0 with complete 714-byte stdout, empty stderr, and cleaned temporary state. Analysis, analysis CLI, base, HTTP, Pro CLI, doctor, settings, and execute/watch/loop handoff smoke passed on both Node majors. The earlier smoke remains valid pre-checklist evidence; smoke r2 is authoritative for the shipped checklist guidance.

**Decisions and boundaries:** no 53rd direct tool exists; exact V1–V5 counts stay `28/31/39/51/52`. Review remains an explicit human/model inspection step and never grants new file, process, Git, credential, network, or approval authority. A complete UI/tool trace was not authorized, so `wrong_tool_calls`, `redundant_tool_calls`, and `total_tool_calls` remain unscored.

**Risks, rollback, and next action:** checklist wording can guide inspection but cannot prove semantic correctness; existing server-local workflow-state and ambient full-access limitations remain unchanged. Roll back by removing the fixed checklist projection/tests and guidance additions, then append another archive correction. No further P4 action is approved; App refresh, Web evidence, deployment, publication, staging/commit/push, credentials, network/services, and unrelated phases remain separately gated.

**Final repository gates:** the post-evidence dual managed build passed on Node 20.20.2/24.15.0; the final workflow/package/documentation set passed 46/46 and the mutation/transaction/registry/pipeline set passed 19/19. `npm run policy:check` and `git diff --check` passed; the latter emitted only the existing informational LF-to-CRLF notices. The strict UTF-8/link scan covered 155 Markdown files and 121 local links with zero invalid files or missing targets. The added-line high-confidence credential scan, staged-file count, deletion count, dependency-file change count, generated-artifact change count, and unindexed archive count were all zero. `Memory.md` remained within its practical limit at 85 lines/15,122 bytes, and active Part 13 remained below its continuation threshold.

## 2026-08-31 — STEP-540: P4 knowledge, rule, and memory reconciliation

**Status:** completed locally under the owner's explicit `neat-freak` cleanup instruction. This is a documentation/rule/memory step; no source behavior, App, runtime, deployment, publication, staging, commit, push, credential, network, service, or unrelated external state was changed. The only project-external write is the authorized Codex automatic-memory correction request described below.

**Goal:** reconcile the completed P4 workflow against current code, user guidance, security guidance, project rules, hot project memory, append-only history, and Codex automatic memory; remove ambiguous or relative wording; and prove the resulting knowledge base remains bounded and internally navigable.

**Files changed:** `AGENTS.md`; `README.md`; `README_ZH.md`; `SECURITY.md`; `design.md`; `CHANGELOG.md`; `docs/CODEXGPT_MASTER_IMPLEMENTATION_PLAN_2026-07-13.md`; `docs/superpowers/specs/2026-08-16-oauth-cross-transport-workspace-capability-design.md`; `Memory.md`; this archive; and the external correction request `D:\Codex\home\memories\extensions\ad_hoc\notes\20260831-153000-codexgpt-p4-closure.md`. Files changed in source: none. All earlier STEP-516–539 working-tree changes were preserved.

**Inventory and rule audit:** reread the project and global `AGENTS.md`, project `Memory.md`, neat-freak instructions/references, root structure, active user docs, current P4 code/schema/registration seams, and the Codex memory registry. Mechanically read all 139 Markdown files under `docs/` (4,647,360 bytes at the initial inventory) and classified every file by size, P4 relevance, relative-time signal, dated-open marker, UTF-8 validity, and link status. Required public entry, mutation inventory, paired specs/plans, archive index, templates, managed toolchains, package version, branch/head, and all 45 archive volumes exist and agree. Project `AGENTS.md` is authoritative and the project does not require or contain an independent `CLAUDE.md`; no symlink was invented. The memory/docs size relationship is healthy, while intentionally frozen historical archives and completed plans above the skill's soft single-file line guideline were left intact rather than destructively repartitioned.

**Corrections and user impact:** current code exposes finite `run_command` in standard tool mode, while P4 verification passes an explicit `mode: full_access` child request and still requires the eligible execution profile plus local Policy/approval. Active P4 guidance had conflated those two axes by saying “full mode”; both READMEs, design, changelog, master plan, and hot memory now state the exact distinction, so users do not widen the visible tool surface unnecessarily. `SECURITY.md` now states that `verify_change` is never automatic or caller-command-selectable but inherits ambient current-user authority when explicitly executed, and that the five-item diff checklist is review guidance rather than isolation or semantic proof. `AGENTS.md` now records the current roadmap P1–P4 local closure as an execution boundary. One OAuth design sentence changed from relative “as today” wording to the exact current `allowedRoots` policy. The append-only STEP-538 wording remains historical; this STEP is its explicit correction.

**Codex memory reconciliation:** the automatic registry had already learned P3 but still said P4 was unauthorized/pending. Because the owner explicitly invoked the memory-cleanup skill, a bounded ad-hoc correction request was added at the required extension path rather than editing generated `MEMORY.md`, `memory_summary.md`, or rollout summaries directly. It records P4's composite-only workflow, exact tool counts, five review obligations, authoritative ordinary/smoke runs, server-local limitation, unscored Web metrics, and external-state boundaries without storing credentials.

**Verification:** `npm run build` passed. `npm run test:focused -- test/change-workflow-contract.test.mjs test/codexgpt-contract.test.mjs test/production-runtime-integration.test.mjs test/package-contents.test.mjs test/auth-documentation.test.mjs test/phase-7-documentation.test.mjs test/tool-description-contract.test.mjs` passed 45/45, including exact composite-only counts, mode projections, immutable registry, public package metadata, and active security/auth documentation. `npm run policy:check` passed. `git diff --check` passed with only existing informational LF-to-CRLF warnings. The full Markdown scan covered 155 files and 121 local links with zero invalid UTF-8, missing targets, or active relative-time matches. All 45 archive volumes are indexed; no file is staged; scoped high-confidence added-line credential hits are zero. The broader repository scan's one candidate is the pre-existing identifier assignment `authToken = environment.CODEBASE_BRIDGE_AUTH_TOKEN ?? environment.CODEBASE_BRIDGE_HTTP_TOKEN`, not a literal credential. The accidental empty untracked file `0)t`, created by a failed audit one-liner at 2026-08-31 14:58 local time, was verified as zero bytes and removed immediately; the scan was rerun with PowerShell-native logic.

**Sizes:** before STEP-540, `AGENTS.md` was 184 lines/15,078 bytes and `Memory.md` was 85 lines/15,122 bytes; both were below project hard limits, with hot memory above the skill's 70% reporting threshold but below the 18 KB practical limit. The active Part 13 archive was 12,566 bytes before this entry, well below its 48 KB continuation threshold. The project docs Markdown layer was about 4.65 MB, so memory is not inverted relative to docs.

**Decisions, risks, and rollback:** no historical archive volume was renamed, repartitioned, or rewritten; no current open item was falsely promoted to scheduled work. `NGROK_DOMAIN`, the separately gated DNS record, background lifecycle, native isolation, external Providers, deferred tasks, App/Web evidence, deployment, and publication remain unchanged. Documentation can describe the authority boundary but cannot replace a real approval or sandbox. Roll back this step by reverting only the active documentation/rule/hot-memory edits and removing the ad-hoc correction request through the authorized memory-extension process; append a new archive correction rather than modifying this entry.

**Next approved action:** none. P1–P4 remain locally closed. Any App refresh, fresh UI/tool trace, runtime deployment, publication, staging/commit/push, credential/network/service change, or deferred implementation still requires separate authorization.

**Post-record verification:** after the STEP-540 project-memory/archive update, the same focused knowledge/contract command again passed 45/45 and `npm run policy:check` again passed. `git diff --check` again reported no error beyond the existing informational LF-to-CRLF notices. The final full-project scan again found 155 Markdown files, 121 valid local links, zero invalid UTF-8 files, zero missing targets, zero active relative-time matches, zero staged files, zero unindexed archive volumes, and zero high-confidence credential literals in the STEP-540 scope or Codex correction note. Final hot-file sizes were `AGENTS.md` 184 lines/15,124 bytes and `Memory.md` 86 lines/15,790 bytes.

## 2026-08-31 — STEP-541: P5 long-task and process-experience closure

**Status:** completed locally under the owner's explicit instruction to execute roadmap P5. No App refresh, ChatGPT Web run, deployment, publication, staging, commit, push, credential, network, service, task-scheduler, or other external-state action was authorized or performed.

**Goal:** make finite commands and persistent/interactive processes present one truthful, resumable lifecycle to V5 callers without creating a second runtime, widening execution authority, or changing older contracts. The user should be able to distinguish a backend that is still starting from one that is actually running, consume output incrementally, and close or revoke a startup without leaking a handle that arrives late.

**Files changed:** `src/tools/schemas/execution.ts`; `src/tools/schemas/codexgpt.ts`; `src/process/processManager.ts`; `src/process/runCommand.ts`; `src/server.ts`; `fixtures/ts-imports/process-experience-contract-imports.ts`; `test/process-experience-contract.test.mjs`; `test/process-manager.test.mjs`; `test/phase-6-model-sequence.test.mjs`; `scripts/test-execution-profile-manifest.mjs`; the paired P5 spec/plan; `CHATGPT_PROMPT.md`; both READMEs; `design.md`; `CHANGELOG.md`; the master plan; `AGENTS.md`; `Memory.md`; and this archive. Earlier STEP-516–540 working-tree changes were preserved.

**Implementation:** added strict additive V5 process output schemas with one canonical `ProcessState`: `starting`, `running`, `exited`, `failed`, or `terminated`. Every successful V5 process response exposes canonical `state` and a retained `status` compatibility alias that must have the same value; V3/V4 keep their previous exact shapes. Persistent records are created as `starting`, become `running` only after backend handle acquisition and the required start audit, and never let `start_process` return successful `starting`. `list_processes` can observe `starting` in V5 while older callers do not receive the new transient state. Startup revoke, explicit terminate, workspace/transport close, expiry, and local emergency termination now join the in-flight start and terminate the eventual owned handle and Windows Job tree. Bounded `run_command`, incremental `output.next_cursor`, cancellable `wait_ms`, quota/retention, redaction, Policy, approval, audit, current-user ambient authority, and the existing process manager remain the only execution path.

**Compatibility and authority:** no direct or composite tool was added or removed; V1/V2/V3/V4/V5 remain exactly `28/31/39/51/52`. `run_command` remains the finite expected-to-exit path available in standard tool mode subject to the execution profile and local gates. Persistent or interactive `start_process` remains full-tool-mode only. Neither path is a sandbox, and P5 adds no filesystem, credential, registry, network, broker, service, or process authority. Cached Apps were not refreshed.

**TDD and focused verification:** the initial focused RED run passed one assertion and failed three because the V5 lifecycle schema, map, and canonical state were absent. After implementation, the P5 contract passed 5/5. A broader local affected set passed 117/117. The managed Node 20.20.2/24.15.0 focused matrix passed 104/104 on each across process lifecycle/output/cursor, bounded command, inherited V3/V4/V5, runtime integration, descriptions, model guidance, and execution-profile inventory. Both managed builds passed. `npm pack --dry-run --json` passed after prepack build and retained package identity `codexgpt@1.0.4` with 694 package files.

**Ordinary and smoke:** final frozen-source detached ordinary run `2026-08-31T18-14-07-141Z-p5-final-ordinary-r1-ea022307` completed exit 0 with complete 486,293-byte stdout, empty stderr, and cleaned temporary state. On each of Node 20.20.2 and Node 24.15.0, fast passed 1249/1250 with one platform skip, safe passed 299/300 with one platform skip, and isolated passed 68/68. Final detached smoke run `2026-08-31T18-35-08-867Z-p5-final-smoke-r2-2a73d195` completed exit 0 with complete 714-byte stdout, empty stderr, and cleaned temporary state; analysis, analysis CLI, base, HTTP, Pro CLI, doctor, settings, and execute/watch/loop handoff smoke all passed on both managed Node majors.

**Failed attempts retained:** a focused process-manager regression showed that local emergency termination returned false for the new `starting` state; the bounded correction admitted owned starting records into the same join-and-terminate path, and the targeted 27/27 suite passed. A direct Node 20 P5 test initially hung because multiple independent TypeScript dynamic imports triggered the repository's known `tsx` import-order problem; three exact owned sessions were interrupted, and the test was changed to one aggregator fixture before passing 5/5 on Node 20. The first final smoke run `2026-08-31T18-32-59-759Z-p5-final-smoke-r1-8ea8b4ca` exited 2 because it incorrectly passed unsupported `--domain smoke` to `test-domains.mjs`, whose valid domains are only `ordinary`, `control`, and `all`; no product test ran. The authoritative r2 run used the repository's actual `npm run smoke` entry and passed.

**Knowledge and rule audit:** the milestone reconciliation enumerated the repository root and all docs Markdown, reread project/global rules and project memory, compared active P5 guidance with schemas/runtime behavior, and checked the archive index. The project intentionally has no independent `CLAUDE.md`, and its rules do not require one. The docs layer remains materially larger than hot memory, so there is no memory/docs inversion. `AGENTS.md` is above the generic skill's approximately 15 KB soft target but below the project's explicit hard boundaries; the single P5 boundary is an operational rule rather than change-history prose, so it was retained. No destructive archive repartition or unrelated cleanup was performed. Because the user did not explicitly authorize changing Codex's generated automatic-memory area, no project-external memory correction file was written.

**Repository gates before knowledge append:** `npm run policy:check`, `git diff --check`, high-confidence credential scanning of the P5 scope, no-staged-file check, exact direct-tool counts, both managed builds, package dry run, and the affected test matrices passed. `git diff --check` emitted only existing informational LF-to-CRLF notices. The working tree remains intentionally dirty with the preserved modernization series and no staged files.

**Risks and limitations:** `starting` is deliberately visible only to V5; legacy V3/V4 callers retain their old wire and cannot distinguish that transient state. Output retention remains bounded, so callers must pass each non-empty `next_cursor` and cannot treat omitted historical bytes as durable logs. Process ownership and Job membership control lifetime but do not isolate current-user ambient authority. The current working tree has no newly authorized complete UI/tool trace, so `wrong_tool_calls`, `redundant_tool_calls`, and `total_tool_calls` remain unscored and no Web-efficiency improvement is claimed.

**Rollback:** remove the additive V5 output map and state alias, restore process records to their previous published state transition, and remove the P5-specific test/guidance/spec/plan changes while retaining STEP-516–518 cursor, cancellation, quota, and owned-tree hardening. Re-run the dual-Node process contract, ordinary, smoke, build, package, and Policy gates. Append any historical correction rather than rewriting this archive entry.

**Next approved action:** none. Roadmap P1–P5 are locally closed. App refresh, fresh ChatGPT Web evidence, runtime deployment, publication, staging/commit/push, credentials, network/services, background installation, and any new product stage remain separately gated.

**Post-record verification:** after the STEP-541 project-memory and archive update, `npm run build` passed and the focused P5/knowledge/package/profile set passed 59/59. `npm run policy:check` passed. `git diff --check` reported no error beyond the existing informational LF-to-CRLF notices. The full repository Markdown audit found 176 files, 123 valid local links, zero invalid UTF-8 files, zero missing targets, and zero unindexed archive volumes across all 45 archives. The active guidance scan found zero relative-time matches; the scoped added-line credential scan found zero high-confidence credential literals; no file is staged or deleted.

## 2026-08-31 — Post-STEP-541 GitHub handoff

**Status:** completed after the owner's explicit request to push the P1–P5 closure to GitHub. This is a publication/handoff record; no new product behavior or authority was added.

**Goal:** publish the already verified local P1–P5 closure on its existing branch while preserving the branch history and the project's no-force/no-merge boundary.

**Files changed:** `Memory.md` and this archive entry only for this post-push record. The handoff published the previously reviewed 120-file closure commit; no unrelated workspace file was added to the handoff.

**Implementation and user impact:** after refreshing `origin/codex/tool-execution-pipeline-slice1` and rerunning the P5 process regression, both managed Node builds, both managed smoke suites, `git diff --check`, the high-confidence credential scan, and `npm run policy:check`, the current staged closure was committed as `731adb7b808b8bc7810a50f875f297674983eca3` with message `feat: close P1-P5 local roadmap implementation`. The normal push updated GitHub from `8a3d5dd3012c7c152fb7eea2fdb3fb91465ebc7e` to that commit. Users and collaborators can now retrieve the verified P1–P5 branch from GitHub; App refresh, runtime deployment, and release publication remain separate actions.

**Verification:** `git push origin HEAD:codex/tool-execution-pipeline-slice1` exited 0; a subsequent ref check reported local `HEAD` and `origin/codex/tool-execution-pipeline-slice1` both at `731adb7b808b8bc7810a50f875f297674983eca3`; `git status --short --branch` reported a clean tracking branch. The post-record documentation checks must pass before this record is committed and pushed.

**Decisions, risks, and rollback:** no force push, merge, tag, release, App mutation, deployment, credential/network/service change, or Web benchmark was performed. The remote branch now contains the reviewed closure and can be reverted only through an explicit new Git revert; history was not rewritten. The unscored Web-efficiency metrics and all separately gated roadmap items remain unchanged.

**Next approved action:** none. Any App refresh, fresh ChatGPT Web evidence, runtime deployment, package/release publication, credentials, network/services, background installation, or new product stage requires separate authorization.

## 2026-09-01 — STEP-542: whole-project verification and dependency remediation

**Status:** local project verification completed; ChatGPT App metadata refresh remains pending one manual UI action. This record documents a user-requested whole-project validation after the P1–P5 handoff, not a new roadmap phase.

**Goal:** validate the already-built project as a whole, identify real failures, repair bounded defects, and verify the repaired runtime without confusing cached ChatGPT App metadata with server behavior.

**Files changed:** `package.json`; `package-lock.json`; `Memory.md`; and this archive entry. No product source, saved OAuth profile, branch ref, credential, deployment, or GitHub remote was changed.

**Defect and bounded fix:** the first production dependency audit found four transitive vulnerabilities: high `brace-expansion`, `fast-uri`, and `ip-address`, plus moderate `hono`. Root `overrides` now pin `brace-expansion@5.0.9`, `fast-uri@3.1.5`, `hono@4.12.34`, and `ip-address@10.3.1`; the lockfile was regenerated. A lock-only install initially left the physical `node_modules` tree on old versions, so the verification correction was `npm ci --ignore-scripts --no-audit`. Actual installed versions then matched the patched lock and `npm audit --omit=dev` reported zero vulnerabilities.

**Verification:** `npm run build` passed. The focused V5 process/cache/runtime set passed 9/9. After the actual dependency reinstall, detached ordinary runs `2026-09-01T06-15-36-357Z-full-regression-installed-node24-cee884d8` and `2026-09-01T06-16-34-668Z-full-regression-installed-node20-1ee65f30` both exited 0 with complete output, empty stderr, cleaned temporary state, and 264/264 ordinary tests. Direct independent-native control runs on Node 24 and Node 20 each passed 113/113. Detached smoke runs `2026-09-01T06-30-20-782Z-full-smoke-installed-node24-97f451c3` and `2026-09-01T06-31-16-231Z-full-smoke-installed-node20-77523d9f` both exited 0 with all eight smoke categories, empty stderr, and cleaned temporary state. `npm pack --dry-run --json --ignore-scripts` retained `codexgpt@1.0.4`, 694 files, and a 1.5 MB package; `npm run policy:check`, `git diff --check`, and the scoped secret-looking diff scan passed.

**Runtime and App evidence:** the saved configured stable launcher was started with process-scoped `CODEXGPT_SEMANTIC_MODE=standard` and `CODEXGPT_TOOL_CONTRACT_VERSION=5`, leaving the saved profile unchanged. Local listeners on 8789/8790 were present; `https://codexgpt.drliang.uk/healthz` returned 200 with `mcpAvailable:true`; exact-host `/mcp` and public `/mcp` without OAuth returned 401; an invalid Host returned 403; and local admin returned 200. The existing cached `codexgpt-Windows-233` App returned `-32603 Internal error` for `server_config` and `list_workspaces` both before and after the healthy V5 restart. The server truthfully warned that a pre-existing 51-tool App needs one **Scan Tools** refresh or recreation. This task has no browser-click capability, so no UI refresh or fresh ChatGPT Web/tool trace was claimed.

**Failed attempts retained:** running detached `npm run smoke` once produced runner-level `spawn npm ENOENT`; invoking the repository smoke script directly with the managed Node executable passed. The first control detached-runner handshake did not create a run directory and was terminated as an owned startup attempt; direct independent-native control execution then passed. A temporary PowerShell quoting error in a version probe was corrected with a native PowerShell manifest read and did not affect the repository.

**Compatibility, authority, and external state:** V1/V2/V3/V4/V5 direct counts remain `28/31/39/51/52`. No App refresh, Web-efficiency metric, deployment, release publication, service installation, credential migration, DNS mutation, commit, or push was performed after the already-pushed `9ab6425` handoff record. The two dependency files are intentionally uncommitted pending review/authorization for the next Git operation; the running V5 service is an explicitly started local/tunnel process and can be stopped with its owned terminal.

**Rollback:** remove the four root overrides and corresponding lockfile resolutions, run `npm ci`, and repeat audit/tests; restore the previous process-scoped service state by stopping the owned terminal. Do not revert unrelated P1–P5 history or the saved OAuth profile.

**Next action:** click **Refresh/Scan Tools** for the existing ChatGPT App on the opened Plugins page, then rerun `server_config`, `list_workspaces`, and `self_test`. Only after that external UI step can App success be confirmed; any commit/push of the dependency fix remains a separate Git authorization boundary.

## 2026-09-01 — Post-STEP-542: App refresh diagnosis and OAuth deployment guard

**Status:** the whole-project local gates remain passed, but App validation is blocked by an OAuth deployment binding conflict that requires explicit external-state authorization. No rebind, App mutation, DNS/tunnel change, commit, or push was performed.

**Goal:** explain the persistent ChatGPT App `-32603 Internal error` and prevent the launcher from silently serving a different canonical workspace than the requested project.

**Evidence and diagnosis:** after the user manually selected Refresh, the cached App still returned `-32603` for `server_config`, `list_workspaces`, and `codexgpt_self_test`. Public `/healthz` was healthy, but the OAuth registry showed `codexgpt.drliang.uk` bound to `D:\Codex\chatgpt上下文插件`, while the intended project and profile are `D:\Dev\codexgpt`. The prior coordinator reused state by identity without validating the canonical root/profile and related public binding fields, so health could be green while the public OAuth runtime served the wrong project.

**Files changed:** `src/auth/deploymentRegistry.ts`; `src/auth/recovery.ts`; `test/phase-8-auth-recovery.test.mjs`; `Memory.md`; and this archive. The earlier STEP-542 `package.json`/`package-lock.json` dependency remediation remains uncommitted and unchanged.

**Implementation and user impact:** added `DeploymentRegistry.assertCompatible` for case-insensitive canonical-root, profile, hostname, issuer, and resource checks; `AuthDeploymentCoordinator.initialize` now invokes it before reusing an existing deployment. The new recovery regression proves a hostname bound to another canonical deployment fails with `OAUTH_STATE_CONFLICT` and leaves the original state unchanged. This prevents cross-project OAuth state reuse, but the intended project cannot start on the currently occupied hostname until the old deployment is explicitly rebound or a new authorized hostname is used.

**Verification:** `npm run test:focused -- test/phase-8-auth-recovery.test.mjs` passed 6/6. `npm run build` passed. The corrected D:\Dev launcher started with V5 environment settings and failed closed with `OAUTH_STATE_CONFLICT`, as designed, instead of serving the other root. The task-owned prior service was stopped after diagnosis; no saved OAuth profile or runtime registry state was modified.

**Decisions, risks, and rollback:** OAuth rebind is a security reset that revokes the old deployment's grants/clients and requires the ChatGPT App to authorize again; a new hostname requires separate tunnel/DNS and App reconnection work. Neither is inferred from a Refresh click. Rollback is to revert only the three source/test files and retain the prior saved deployment state; do not weaken the compatibility check or silently rebind.

**Next action:** obtain explicit authorization for either (A) rebinding `codexgpt.drliang.uk` from the old canonical root to `D:\Dev\codexgpt`, with grant revocation and App reauthorization, or (B) using a separately authorized hostname/tunnel for `D:\Dev\codexgpt`. Only after that can the service restart and the complete App/project validation continue.

## 2026-09-01 — Authorized OAuth rebind and target runtime restart

**Status:** the user authorized option A. The stable hostname was rebound to `D:\Dev\codexgpt` as a security reset, the target V5 runtime is healthy, and final ChatGPT App validation is pending relink/reauthorization in the user session.

**Goal:** make the existing `codexgpt-Windows-233` App address the intended project after the cross-root OAuth binding diagnosis, while preserving the stable hostname and Cloudflare route.

**Preflight and bounded CLI correction:** the first authorized rebind attempt failed before mutation because the target profile contained an exact stale copy of the source OAuth selectors left by the earlier cross-root reuse. `scripts/oauth-admin.mjs` now recognizes that case only when all required tunnel, hostname, port, OAuth issuer/resource, credential-provider, and state-reference selectors match the source exactly; a different or partially matching configured target still fails closed. The CLI test covers positive and negative alias cases.

**External state change and user impact:** `codexgpt auth rebind --from-root D:\\Codex\\chatgpt上下文插件 --root D:\\Dev\\codexgpt --hostname codexgpt.drliang.uk --revoke-all` completed successfully. The registry and profiles now assign the hostname to `D:\Dev\codexgpt`; the old deployment's clients, grants, and tokens were revoked; the Cloudflare route was not changed. The target profile is OAuth-enabled and the source profile is retained as Legacy. The ChatGPT App must relink/reauthorize because the security reset intentionally leaves no active client or grant.

**Runtime evidence:** the saved target launcher restarted with process-scoped V5 settings and reported the intended workspace. `auth status --root D:\Dev\codexgpt --json` showed the target runtime active with no clients or grants. Public `/healthz`, protected-resource metadata, and authorization-server metadata returned 200; public `/mcp` without a bearer returned 401; an invalid Host returned 403. App calls made before relink still returned ChatGPT's generic `-32603 Internal error`, which is expected while the old App authority is absent and is not evidence of a target server failure.

**Verification:** managed Node 20 and Node 24 focused CLI/recovery tests passed 28/28 on each. `npm run build`, `npm run policy:check`, and `git diff --check` passed. Earlier whole-project STEP-542 ordinary/control/smoke, package, audit, and runtime gates remain passed; this step added the stale-alias CLI guard and revalidated its affected dual-Node tests.

**Decisions, risks, and rollback:** this was the explicitly selected OAuth security reset. It changes local protected OAuth registry/profile state and invalidates the old App authority, but does not change DNS, Tunnel ingress, release data, Git refs, or remote state. Rollback requires a separately reviewed rebind back to the old canonical root and another forced relink; do not restore old grants or weaken the exact-alias guard.

**Next action:** in the open ChatGPT App page, select **Refresh/Scan Tools**. If the old client remains invalid, remove and recreate the development App using the unchanged URL `https://codexgpt.drliang.uk/mcp`, then authorize it again. After that, rerun a new conversation's representative read, context, and bounded write/execute confirmation tests. No commit or push has been performed for the current working-tree changes.

## 2026-09-01 — Post-rebind ChatGPT App relink and read-only acceptance

**Status:** completed for OAuth reconnection and representative Web read-only acceptance. This is not a benchmark run and does not score Web-efficiency metrics.

**Goal:** replace the stale ChatGPT OAuth client left invalid by the authorized root rebind, prove the newly bound `D:\Dev\codexgpt` service can be authorized by ChatGPT Web, and capture a real workspace tool result without changing the workspace.

**Files changed:** `Memory.md` and this append-only archive entry. The user created/published the external ChatGPT App `codexgpt-Windows-v2`; no source, saved profile, Cloudflare route, DNS record, package, Git ref, commit, or push changed in this acceptance step.

**Implementation and user impact:** the first same-name App creation was rejected because `codexgpt-Windows` was already occupied. A non-destructive replacement named `codexgpt-Windows-v2` was created with the token-free public MCP URL and OAuth discovery. ChatGPT dynamically registered a new public client, the user published and connected that replacement, and local owner approval completed its PKCE authorization. The user then sent the requested read-only workspace test. The visible ChatGPT tool trace showed one earlier `open_current_workspace` call and a succeeding `git_status` call with `ok: true`, the intended `D:\Dev\codexgpt` root, the current branch, and the current dirty-file inventory. No workspace mutation was requested or observed.

**Verification:** after restarting the existing saved local/tunnel runtime, `auth status --json` reported the runtime active. Local `auth pending` exposed one ChatGPT request with read/write/execute scopes; `auth approve` completed it. A subsequent status reported the client `approved`, one active grant, and a completed token exchange/refresh rotation. The append-only OAuth audit recorded client registration, authorization request, client/authorization approval, authorization-code creation, exchange, and refresh rotation. The attached ChatGPT Web screenshot supplies the authoritative UI/tool trace for `open_current_workspace` plus `git_status`; it is more direct evidence of tool invocation than token-exchange status alone. `git diff --check` reported no error, only pre-existing LF-to-CRLF notices.

**Decisions, risks, and rollback:** the legacy `codexgpt-Windows-233` draft was deliberately not published, deleted, or renamed. The new App remains `codexgpt-Windows-v2` because that preserves the old draft and avoids another credential reset. The grant has read/write/execute scopes, but this acceptance used only reads; every later write or process execution remains subject to the existing Policy/approval boundaries. Rollback is to revoke the replacement grant/client locally and disable or delete the replacement App only with explicit approval; do not restore the stale client or weaken root compatibility checks.

**Next action:** none for OAuth/relink acceptance. A separate authorization is required to clean up/rename legacy Apps, to run a benchmark-grade Web trace, or to perform a P5 `start_process` execution through ChatGPT Web. No commit or push is authorized for the uncommitted STEP-542/rebind/documents worktree.

## 2026-09-01 — Windows + ChatGPT daily-operation guide

**Status:** completed documentation-only operational handoff. No source, OAuth profile, client/grant, Tunnel, DNS, App, process, Git ref, commit, or push was changed.

**Goal:** give the user a repeatable, credential-safe Chinese Markdown procedure for normal Windows reboot recovery, safe workspace use, OAuth reapproval, and first-line troubleshooting after the successful `codexgpt-Windows-v2` read-only Web acceptance.

**Files changed:** `docs/CODEXGPT_WINDOWS_CHATGPT_DAILY_OPERATION_GUIDE_ZH.md`; `Memory.md`; and this append-only archive entry.

**Implementation and user impact:** the guide makes the operational fact explicit: restart of Windows or closure of the foreground terminal stops the local MCP runtime and named Tunnel, so the user restarts the supported entry with the exact `D:\Dev\codexgpt` root. It records that normal foreground restart retains OAuth issuer/binding/Tunnel/client/refresh-family state, while the runtime-scoped workspace capability itself is renewed after restart. It separates temporary, exact `--allow-root` plus explicit `open_workspace` usage from cross-root default deployment rebind, which remains a security-sensitive external decision. It also provides an owner-approval path, non-destructive health/status/doctor diagnostics, the v2 App identity, and explicit no-go actions for credentials, broad roots, unknown processes, automatic background installation, and legacy-App cleanup.

**Verification:** live `auth status --root D:\Dev\codexgpt --json` reported the expected OAuth profile, public hostname, active runtime, approved ChatGPT client, and active grant. The command and workspace semantics were cross-checked against `README_ZH.md`: normal foreground restart, exact-root Scan Tools condition, process shutdown hotkey, configured-root workspace capability/restart expiry, `open_workspace` pattern, and full-access process boundary. `npm run policy:check` and documentation/diff checks remain required after this record.

**Decisions, risks, and rollback:** the guide intentionally does not present `--allow-root` as a permanent default-root change and does not claim a successful Web write, long-process, or efficiency test. It does not include token material or authorization URLs. Revert only the three documentation/index additions if the guide wording needs replacement; do not alter live OAuth state or remove legacy App data.

**Next action:** use the guide after the next reboot. Automatic startup, separate long-lived project deployments, App cleanup, hostname rebind, and benchmark-grade Web testing remain separately authorized decisions.

**Verification correction:** after the guide and index update, `npm run policy:check` passed (`Repository operational policy: PASS`); `git diff --check` found no whitespace error (only existing LF-to-CRLF notices); a structural check found 20 balanced Markdown fences, all required operational topics present, and zero matches for token, bearer, client-secret, refresh-token, or private-key patterns in the new guide.
