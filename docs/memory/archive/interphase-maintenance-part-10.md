# Interphase Maintenance Archive — Part 10

Continuation of the append-only interphase record after Part 9 was sealed at STEP-505. This volume begins with STEP-506 and remains active until the repository memory protocol closes it.

## 2026-08-16

### STEP-506 F2 mixed-change review pair

**Status / goal:** F2 completed matched successor-adjusted baseline/candidate Web execution and awaits post-result review. Two fresh fixed-ref targets received the same uncommitted seed: a harmless README wording change and `OutputRing.read()` changed to report `truncated: false` unconditionally. The fixed read-only prompt asked each variant to prioritize the blocking issue and name the narrowest verification.

**Results / scoring:** Both assistants classified README as low-risk documentation, blocked the OutputRing regression, explained that evicted output would be silently presented as continuous, named and ran `test/process-output-ring.test.mjs`, observed the expected 4/5 failure at `page.truncated`, and left both seed files unstaged and otherwise unchanged. Both score task success, one context fetch, and read-only verification `null`. Both cumulative activity panels mispair semantic inputs and outputs, so wrong/redundant/total/retries and pair efficiency remain `null`; `p1_effect_for_f2=not_demonstrated`.

**Runtime / files / rollback / next:** Baseline target `codexgpt-1cca8bde` and candidate target `codexgpt-3c08d0d9` retain only the identical two-file seed. Runs `2026-08-16T21-48-14-915Z-step506-f2-baseline-successor-64062279` and `2026-08-16T21-51-07-535Z-step506-f2-candidate-successor-1ecb2efc` are exact stopped/stop-recorded with zero listeners. Added three F2 JSON files and Part 10, updated README/`Memory.md`, and changed no retained server/product source, credential, OAuth/DNS/Tunnel state, commit, push, or publication. Preserve evidence and append corrections. After review, G1 is next; no later implementation phase is authorized.

#### STEP-506 final closure gate

Three reviews found no P0–P3 defect. Frozen Node benchmark contract/runtime tests passed 12/12 and policy passed; JSON assertions, exact refs/overlay/identical seed/run linkage, target diffs, stopped cleanup, repository diff check, and credential-value scan passed. STEP-506 is fully closed with task success tied and efficiency unscored under both mispaired traces. G1 is next.

## 2026-08-17

### STEP-507 G1 OutputRing retained-byte capability pair

**Status / goal:** G1 completed matched successor-adjusted baseline/candidate ChatGPT Web execution and awaits post-result review. Each variant received a separate fresh detached clean target at the fixed `c43ec8e...` ref and the unchanged prompt required a direct failing test before a minimal `OutputRing.retainedBytes()` implementation, focused verification, build, and diff review.

**Results / scoring:** Both assistants observed the expected RED `TypeError` before adding the same four-line accessor returning the existing `#retained` count. Baseline added `test/process-output-ring-retained-bytes.test.mjs` after content protection rejected edits to the secret-fixture test; candidate safely added equivalent zero/UTF-8/eviction assertions to the existing test. Frozen CodexPro Node independently passed the baseline direct test and candidate 6/6 suite, TypeScript build, and `git diff --check`; each target changes only source plus one direct test. Both score task and verification success with one context fetch. Both activity panels omit reported verification calls, and baseline also mispairs the latest edit input with a show-changes output, so wrong/redundant/total/retries and pair efficiency remain `null`; `p1_effect_for_g1=not_demonstrated`.

**Runtime / files / rollback / next:** Baseline target `codexgpt-22269361` and candidate target `codexgpt-cd470749` remain unstaged with their intended two-file changes. Runs `2026-08-16T21-56-40-989Z-step507-g1-baseline-successor-9282864c` and `2026-08-16T22-04-19-948Z-step507-g1-candidate-successor-4e3137df` are exact stopped/stop-recorded at `2026-08-16T22:02:39.795Z` and `2026-08-16T22:09:14.476Z`; listeners are zero. Added three G1 evidence JSONs and updated README/`Memory.md`/Part 10; no retained server/product source, credential, OAuth/DNS/Tunnel state, commit, push, or publication changed. Preserve evidence and append corrections; after review G2 is next, while later implementation phases remain gated.

#### STEP-507 final closure gate

Three post-result reviews found no P0/P1/P3 defect. A P2 machine-readable control gap was corrected by binding the pair to exact Node `24.15.0`, OAuth auth mode, configured-root `oauth_cross_transport`, both fixed server refs, fresh chats, and identical empty setup. Frozen Node benchmark contract/runtime tests passed 12/12 and policy passed; final JSON assertions, exact refs/overlay/run linkage, target tests/build/diffs, stopped cleanup, repository diff check, credential-value scan, Memory limits, and append-only review passed. STEP-507 is fully closed with task and verification tied, while incomplete traces leave efficiency/retries unscored. G2 is next.

### STEP-508 G2 bounded redaction-helper pair

**Status / goal:** G2 completed matched successor-adjusted baseline/candidate Web execution and awaits post-result review. Separate fresh detached clean targets at fixed `c43ec8e...` received the unchanged prompt to test line bounding, redaction reuse, and positive-integer validation before adding exported `redactSensitiveLines` and running focused verification/build/diff review.

**Results / scoring:** Both assistants obtained the expected missing-helper RED, validated line bounds/redaction/invalid values, and reused `redactSensitiveText` without duplicating patterns. Baseline safely rewrote a secret-looking test literal at runtime after content protection rejected it, used LF bounding, and independently passed 6/6. Candidate covered LF/CRLF/CR preservation and independently passed 8/8. Frozen Node build and diff checks passed for both; each target changes only `src/redact.ts` plus new `test/redact-sensitive-lines.test.mjs`. Both score task/verification success and one context fetch. Both activity panels mispair their final input/output and omit material rows, so wrong/redundant/total/retries and pair efficiency remain `null`; `p1_effect_for_g2=not_demonstrated`.

**Runtime / files / rollback / next:** Targets are `codexgpt-34f61f08` and `codexgpt-4989f9c6`, unstaged with intended two-file diffs. Runs `2026-08-16T22-16-05-516Z-step508-g2-baseline-successor-0cd1a9fd` and `2026-08-16T22-21-25-560Z-step508-g2-candidate-successor-0289cfbf` are exact stopped/stop-recorded at `2026-08-16T22:20:53.169Z` and `2026-08-16T22:26:00.500Z`, with zero listeners. Added three G2 JSONs and updated README/`Memory.md`/Part 10; no retained server/product source, credential, OAuth/DNS/Tunnel state, commit, push, or publication changed. Preserve evidence and append corrections; after review H1 is next, while later implementation phases remain gated.

#### STEP-508 scoring correction and final closure gate

Post-result adversarial review supersedes the initial task-success tie above. The baseline helper consumes only LF; frozen-Node counterexamples with `maxLines=2` show LF input bounded to two lines, but CRLF input becomes `a\r\nb\r` and CR input remains `a\rb\rc`, so the Windows newline bound is not satisfied. Baseline is therefore `task_success=false` with `verification_completed=true`; candidate remains successful, yielding `candidate_advantage_observed_single_run` without claiming statistical or general causality. Three reviews otherwise passed runtime/fairness/cleanup, fixed-contract scoring, partial-trace null boundaries, and documentation consistency. Frozen Node benchmark contract/runtime tests passed 12/12 and policy passed; final JSON assertions, exact refs/overlay/run linkage, stopped cleanup, repository diff check, credential-value scan, Memory limits, and append-only review passed. STEP-508 is fully closed; H1 is next.

### STEP-509 H1 persistent-process lifecycle pair

**Status / goal:** H1 completed matched successor-adjusted baseline/candidate Web execution and awaits post-result review. Separate fresh detached clean targets at fixed `c43ec8e...` received the unchanged prompt to start the 120-second fixture through a persistent process path, read ready plus a later incremental state, explicitly terminate it, and confirm terminal state without a bounded two-minute wait.

**Results / scoring:** Both App sessions exposed the same legacy 27-tool surface without `start_process`, `read_process_output`, or `terminate_process`. Baseline inspected capabilities, then used Bash controllers; its first log-path attempt required cleanup before a stable-log retry observed `ready:11852`, an empty delta, SIGTERM, and `alive=false`. Candidate used a shorter Bash/Node controller to observe `ready:10712`, empty incremental stdout, SIGTERM, and a separate `still_running=false` check. Independent process queries found no fixture child. Both therefore fail the persistent-process-path criterion but complete lifecycle verification: `task_success=false`, `verification_completed=true`. Both activity panels omit material actions and mispair the last visible input/output, so wrong/redundant/total/retries and pair efficiency remain `null`; `p1_effect_for_h1=not_demonstrated`.

**Runtime / files / rollback / next:** Targets `codexgpt-8f9fc6cd` and `codexgpt-cc2fdecb` remain detached, clean, and exact. Runs `2026-08-16T22-35-42-620Z-step509-h1-baseline-successor-96404e49` and `2026-08-16T22-41-57-267Z-step509-h1-candidate-successor-effcbae0` are exact stopped/stop-recorded at `2026-08-16T22:41:21.244Z` and `2026-08-16T22:45:34.425Z`; fixture and listener counts are zero. Added three H1 JSONs and updated README/`Memory.md`/Part 10; no retained server/product source, credential, OAuth/DNS/Tunnel state, commit, push, or publication changed. Preserve evidence and append corrections; after review H2 is next.

#### STEP-509 context-fetch correction and final closure gate

Scoring review found that the initial context count omitted the capability inventory: baseline configuration inventory and candidate `codexgpt_inventory` are context-only startup calls, so both variants and the pair now record two context fetches rather than one. Three post-result reviews otherwise found no P0-P3 defect: fallback controllers do not satisfy the persistent-process seam, but their incremental observation, explicit termination, and independent zero-process check support completed verification. Frozen Node benchmark contract/runtime tests passed 12/12 and policy passed; final JSON assertions, exact refs/overlay/run linkage, clean targets, stopped cleanup, repository diff check, credential-value scan, Memory limits, and append-only review passed. STEP-509 is fully closed; H2 is next.

### STEP-510 H2 long process plus targeted inspection pair

**Status / goal:** H2 completed matched successor-adjusted baseline/candidate Web execution and awaits post-result review. Separate fresh detached clean targets at fixed `c43ec8e...` received the unchanged prompt to observe the 120-second fixture while live, read only the relevant fixture, explain its event-loop lifetime, and terminate it early without waiting two minutes.

**Results / scoring:** Both assistants correctly explained that the default ref'ed `setTimeout` keeps Node alive until its callback prints `completed`, after which no active work remains. However, both first used bounded Bash/PowerShell execution whose nominal three-second timeout returned only after about 120.5/120.6 seconds; both also inspected runner implementation/tests beyond the fixture. Later controlled detached instances produced ready/alive evidence, received SIGTERM, and yielded cleanup confirmation, independently corroborated by zero fixture processes. Both therefore score `task_success=false`, `verification_completed=true`, two context fetches, and `null` wrong/redundant/total/retries under incomplete or mispaired activity traces; `p1_effect_for_h2=not_demonstrated`.

**Runtime / files / rollback / next:** Targets `codexgpt-cc6fcf2e` and `codexgpt-37a0698f` remain detached, clean, and exact. Runs `2026-08-16T22-52-49-343Z-step510-h2-baseline-successor-26854d5e` and `2026-08-17T05-09-54-560Z-step510-h2-candidate-successor-b0b85cd2` are exact stopped/stop-recorded at `2026-08-17T05:08:48.180Z` and `2026-08-17T05:23:01.840Z`; fixture and listener counts are zero. Added three H2 JSONs and updated README/`Memory.md`/Part 10; no retained server/product source, credential, OAuth/DNS/Tunnel state, commit, push, or publication changed. Preserve evidence and append corrections; after review I1 is next, while later implementation phases remain gated.

#### STEP-510 attribution correction and final closure gate

Three post-result reviews found no P0–P2 defect. One P3 attribution was corrected: the depth-three tree bundled into the mandatory candidate `open_workspace` response is an observation, not a separate model-selected failure; the full-duration bounded wait and active runner/test reads independently establish task failure. Both task failures, completed verification, two context fetches, partial-trace null metrics, exact refs/overlay/Node/allow-root controls, clean targets, stopped runtimes, and zero fixture/listener residue passed review. Frozen Node benchmark contract/runtime tests passed 12/12 and policy passed; final JSON assertions, repository diff check, credential-value scan, Memory limits, and append-only review passed. STEP-510 is fully closed; I1 is next.

### STEP-511 I1 single-file change-set rollback pair

**Status / goal:** I1 completed matched successor-adjusted baseline/candidate Web execution and awaits post-result review. Separate fresh detached clean targets at fixed `c43ec8e...` received the unchanged prompt to add one temporary README marker, inspect the returned change set/diff, undo that exact change set without Git reset/checkout, and prove the original state.

**Results / scoring:** Both App sessions exposed the same legacy 27-tool surface without `undo_change_set`, while edit did not yield a change-set identifier. Baseline first missed an exact edit, then produced the requested one-line diff, explicitly confirmed `ACTION_NOT_AVAILABLE`, and used a prohibited manual inverse edit before proving the original README hash and empty diff. Candidate inspected the surface and implementation, declined to mutate because required rollback was unavailable, and proved the target remained clean. Both therefore score `task_success=false`, `verification_completed=true`, two context fetches, and `null` wrong/redundant/total/retries under incomplete grouped activity; `p1_effect_for_i1=not_demonstrated`.

**Runtime / files / rollback / next:** Accepted targets `codexgpt-56717464` and `codexgpt-963eaff6` remain detached, clean, and exact; README blobs equal HEAD. Accepted runs `2026-08-17T05-29-03-423Z-step511-i1-baseline-successor-f029dd9a` and `2026-08-17T05-37-37-207Z-step511-i1-candidate-successor-0d0636e1` are exact stopped/stop-recorded at `2026-08-17T05:33:05.069Z` and `2026-08-17T05:43:17.324Z`; listeners are zero. A candidate message accidentally split at the blank line before its case body; unscored run `2026-08-17T05-34-01-394Z-step511-i1-candidate-successor-098248b6` was exact-stopped, its target stayed clean, and a fresh target/runtime/chat replaced it. Added three I1 JSONs and updated README/`Memory.md`/Part 10; no retained server/product source, credential, OAuth/DNS/Tunnel state, commit, push, or publication changed. Preserve evidence and append corrections; after review I2 is next, then the Phase 0 aggregate/review, while later implementation phases remain gated.

#### STEP-511 final closure gate

Three post-result reviews found no scoring, runtime, fairness, cleanup, or documentation defect except a machine-readable pair-schema issue: unsupported retry evidence is now `null`, explicit wrong/redundant/total nulls are present, and fairness keys match the established pair schema. Frozen Node benchmark contract/runtime tests passed 12/12 and policy passed. Final JSON parsing/assertions, exact refs/overlay/Node/allow-root linkage, three clean exact targets, accepted and superseded exact-stop evidence, zero listeners, repository diff check, credential-value scan, Memory/Part 10 limits, and append-only review passed. STEP-511 is fully closed; I2 is next, then the Phase 0 aggregate/review, with later implementation phases still gated.

### STEP-512 I2 multi-file atomic rollback pair

**Status / goal:** I2 completed matched successor-adjusted baseline/candidate Web execution and awaits post-result review. Separate fresh detached clean targets at fixed `c43ec8e...` received the unchanged prompt to use one atomic patch for matching README/FAQ markers, inspect the diff, undo the returned change set, and prove both files restored.

**Results / scoring:** Both assistants used one successful atomic multi-file patch after two preflight-rejected attempts, then reviewed only README/FAQ at `+4/-0`. The frozen App exposed neither a usable returned change-set identifier nor a change-set undo action, so both used prohibited atomic inverse patches. Independent Git status and blob checks prove both files restored exactly. Both therefore score `task_success=false`, `verification_completed=true`, two context fetches, and `null` wrong/redundant/total/retries under incomplete grouped activity; `p1_effect_for_i2=not_demonstrated`.

**Runtime / files / rollback / next:** Targets `codexgpt-a964a55b` and `codexgpt-0e2f16c1` remain detached, clean, and exact; README/FAQ blobs equal HEAD. Runs `2026-08-17T05-49-52-951Z-step512-i2-baseline-successor-a96b04e0` and `2026-08-17T05-56-49-407Z-step512-i2-candidate-successor-4597a8cd` are exact stopped/stop-recorded at `2026-08-17T05:56:16.424Z` and `2026-08-17T06:02:54.875Z`; listeners are zero. Added three I2 JSONs and updated README/`Memory.md`/Part 10; no retained server/product source, credential, OAuth/DNS/Tunnel state, commit, push, or publication changed. Preserve evidence and append corrections; after review only the Phase 0 aggregate/review remains, while later implementation phases stay gated.

#### STEP-512 final closure gate

Three post-result reviews found no P0-P3 scoring, runtime, fairness, cleanup, schema, append-only, or documentation defect. They confirmed that atomic inverse patches do not satisfy returned-change-set undo, while final clean/blob identity supports completed verification; partial grouped activity keeps wrong/redundant/total/retries `null`. Frozen Node benchmark contract/runtime tests passed 12/12 and policy passed. Final JSON parsing/linkage, exact refs/overlay/Node/allow-root, clean targets, exact-stop evidence, zero listeners, repository diff check, credential-value scan, Memory/Part 10 limits, and append-only review passed. STEP-512 is fully closed; only the Phase 0 aggregate/review remains, with later implementation phases gated.

### STEP-513 Phase 0 aggregate

**Status / goal:** All 18 fixed cases now have formal execution evidence; the Phase 0 aggregate draft is complete and awaits post-result review. A1 remains a shared successor continuity gate rather than a matched P1 pair, so variant KPI denominators use the 17 controlled pairs A2-I2.

**Results:** Baseline task success is `11/17` (64.71%); candidate is `12/17` (70.59%), with G2 the only asymmetric outcome. Both complete verification in all 12 required cases (100%) and both average `21/17` context fetches (1.2353). Wrong Tool Call Rate and mean total calls remain `null`: complete trace coverage is baseline `0/17` versus candidate `2/17`, so candidate's observed `0/8` subset is not a matched campaign KPI. Sparse retry evidence likewise leaves mean retries `null`. The single-run `+1/17` task observation is not statistical proof or a demonstrated efficiency effect.

**Files / risks / rollback / next:** Added `docs/benchmarks/chatgpt-web-e2e/runs/2026-08-17-phase0-aggregate.json` and updated benchmark README, `Memory.md`, and Part 10; no product source, retained runtime, target, credential, OAuth/DNS/Tunnel state, commit, push, or publication changed. The frozen App's legacy 27-tool surface materially limits H/I interpretation, and missing full traces block efficiency acceptance. Preserve formal evidence and append corrections rather than deleting history. After final review, stop Phase 0 and request a separate decision on trace recollection or further P1 work; do not enter later phases.

#### STEP-513 final Phase 0 closure gate

Three aggregate reviews independently reproduced baseline `11/17`, candidate `12/17`, verification `12/12` each, context `21/17` each, the 11 tie-pass / 5 tie-fail / 1 candidate-only partition, trace coverage `0/17` versus `2/17`, and retry coverage 5 versus 6 known cases. One P2 provenance gap was corrected by embedding fixed controls and all 17 pair evidence/review links; one stale README date was corrected. Re-review found no P0-P3. Frozen Node benchmark contract/runtime tests passed 12/12 and policy passed; aggregate/linkage assertions, all-stopped runner state, zero benchmark/listener/fixture residue, JSON parse, repository diff check, credential-value scan, Memory/Part 10 limits, and append-only review passed. Phase 0 is complete and stops here; trace recollection or any further P1/later-phase work requires a separate decision.

### STEP-514 Phase 0 knowledge and rules reconciliation

**Status / goal:** Initial documentation cleanup is complete and awaits post-result adversarial review. Reconcile the completed Phase 0 campaign with the concise project index and mechanically audit the active rules/documentation surface without changing product code, benchmark evidence, retained targets, runtimes, credentials, or external state.

**Files changed / implementation:** Updated `Memory.md` in place: replaced the stale authorization that said Phase 0 could continue, advanced the current state through STEP-514, and collapsed ten superseded per-case summaries into the authoritative STEP-513 aggregate plus this maintenance summary. Kept historical details in the append-only Parts 8–10 and formal run JSONs. No change was needed in `AGENTS.md`, root user/security docs, or the benchmark README because their current paths and Phase 0 status remain aligned.

**Verification / decisions / risks / rollback / next:** Pre-review checks found no broken relative Markdown links across the active rule/index/root/benchmark documents; every critical `AGENTS.md` script/spec/plan path exists; required `policy:check`, `test:focused`, and `task:run` package scripts exist. `Memory.md` was the only active index near its practical size ceiling, so detailed case history stays archived rather than duplicated. Closed historical volumes remain untouched and Part 10 remains below 48,000 bytes. Rollback is an in-place `Memory.md` correction plus an append-only archive correction, never deletion or rewrite of completed benchmark evidence. Post-result rule, documentation, runtime, test, policy, diff, credential, size, and append-only review follow; no later phase is authorized.

#### STEP-514 changed-files correction and final closure gate

The initial changed-files paragraph above omitted this archive from its own list. The complete STEP-514 change set is `Memory.md` plus `docs/memory/archive/interphase-maintenance-part-10.md`; no product, test, benchmark evidence, retained target, runtime, credential, OAuth/DNS/Tunnel state, commit, push, or publication changed. This appended correction supersedes only that incomplete file list and preserves the original entry.

Three post-result adversarial reviews found no P0/P1 defect. Their shared P2 observation—that `Memory.md` could only call STEP-514 reviewed after this gate—closes here; one P3 reviewer requested exact size disclosure, and the runtime reviewer identified the corrected changed-files omission. Runtime inspection found 40 retained benchmark runs all stopped, no active owned run, zero 8789/8790 listeners, and no CodexGPT entry, runner worker, or long-lived fixture residue, so no cleanup action was taken.

Frozen CodexPro Node `v24.15.0` ran `npm run test:focused -- test/chatgpt-web-benchmark-contract.test.mjs test/chatgpt-web-benchmark-runtime.test.mjs` at 12/12 PASS and `npm run policy:check` at PASS. A preliminary direct call to nonexistent `scripts/run-focused-tests.mjs` failed with `MODULE_NOT_FOUND`; the authoritative package script was then used successfully and no file/state change resulted. Machine checks passed aggregate status/18 fixed cases/17 matched reviewed pairs and every pair/review path, all 146 project Markdown relative links, critical rule paths and package scripts, JSON parsing, `git diff --check` apart from existing LF-to-CRLF warnings, credential-value scan, and unchanged Git HEAD/base refs. The only relative-time scan hit was the design phrase “exactly as today,” which describes preserved current behavior rather than a dated promise. Final pre-gate sizes were `Memory.md` 114 lines / 14,999 bytes, `AGENTS.md` 184 / 15,078, and Part 10 111 / 23,275; all remain below project limits and Part 10 requires no rollover. STEP-514 is fully reviewed and closed. Phase 0 remains stopped; trace recollection, App refresh, another P1 slice, or any later phase requires a separate owner decision.

### STEP-515 clarify the `search` lexical/semantic contract

**Status / goal:** Completed and reviewed. Implement the smallest evidence-selected P1 improvement after Phase 0: make the user-visible `search` description tell ChatGPT when lexical search is appropriate and prevent lexical hits from being presented as certain semantic definitions/references. Do not alter ToolExecutionPipeline, tool names, schemas, scopes, handlers, OAuth/workspace boundaries, or later-phase architecture.

**Files changed / implementation:** The STEP-515 logical delta touches `src/server.ts`, `test/search-contract.test.mjs`, and `test/production-runtime-integration.test.mjs`; those files also contain older intentional working-tree changes that are outside this step. The `search` descriptor now routes exact text, strings, error messages, configuration keys, and lexical symbol occurrences to `search`; unknown filename/directory discovery to `tree`; and definitions, references, diagnostics, or rename impact to `semantic` only when that tool is available. It explicitly forbids treating lexical matches as semantic certainty and prefers one targeted search over broad repetition. The direct contract test freezes the exact text. Because descriptions are part of the V1 wire descriptor snapshot, the reviewed standard/full/connection descriptor hashes were intentionally refreshed; tool names, title, schemas, annotations, direct/supertool payload hashes, handler, and pipeline remain unchanged.

**TDD / verification:** Frozen CodexPro Node `v24.15.0` first ran `npm run test:focused -- test/search-contract.test.mjs`: the new exact assertion produced the intended single RED at 16/17 against the old description. After the source change, the initial 24/24 search/pipeline suite, TypeScript build, and policy passed. Adversarial review then found two real gaps: the first wording falsely claimed filename lookup although search only matches file contents, and existing V1 descriptor snapshots were not refreshed. The filename claim was replaced with explicit `tree` routing. `npm run test:focused -- --test-name-pattern "contract V1 wire snapshots" test/production-runtime-integration.test.mjs` then produced the expected snapshot RED with only standard/full/connection descriptor hashes changed; those three reviewed hashes were updated. Final `npm run test:focused -- test/search-contract.test.mjs test/tool-execution-pipeline.test.mjs test/production-runtime-integration.test.mjs` passed 40/40, `npm run build` passed, `npm run policy:check` passed, `git diff --check` reported only existing LF-to-CRLF warnings, and the scoped credential-value scan passed. One reviewer saw the oversized-ripgrep case fail under an unwrapped direct `node --test`; the required cleanup-wrapped focused harness passed that case in both final runs, so no product change was made for the unrelated runner observation.

**Decisions / risks / rollback / next:** Three post-result reviews verified the D2 fixed contract, conditional V1–V4 semantic wording, wire compatibility boundary, and exact logical scope. This change improves tool selection guidance but does not by itself prove lower wrong/redundant-call rates; no benchmark or statistical claim is added. Rollback only the STEP-515 description assertion/source text and restore the three prior descriptor hashes—never revert whole dirty files or earlier STEP-493/P1 changes. No commit, staging, push, deployment, App refresh, credential, OAuth/DNS/Tunnel, retained runtime, or target state changed. STEP-515 stops here; any further P1 slice, trace recollection/App refresh, or later phase requires a separate owner decision.

#### STEP-515 wire-snapshot correction and final re-review

The closure text above captured descriptor hashes from the intermediate wording that still mentioned filenames. After the final wording routed unknown filename/directory discovery to `tree`, a fresh cleanup-wrapped snapshot check correctly failed because the descriptor bytes changed again. This correction supersedes the three intermediate expected hashes, not the implementation decision or other evidence: final standard/full/connection descriptor hashes are respectively `73d0ff8fb1adfd08b60af9e8448c20697687ca789ae8e6da0bfc56965575f29c`, `384e71c76ed689d8286d6c51869775e03cae400c772542e49df39021ac4a81e1`, and `f845b31b7aa1df00141a72eb3e0d1661f869b72b60e3645230edd8eb1ca7c070`. Minimal and direct/supertool hashes remain unchanged.

After updating those reviewed final hashes, frozen CodexPro Node `v24.15.0` reran the authoritative three-file focused command at 40/40 PASS, TypeScript build PASS, repository policy PASS, diff check with only existing line-ending warnings, and scoped credential scan PASS. Independent re-review reproduced 40/40 and found no remaining P0–P3; the earlier reviewer findings were sampled before this correction. The append-only record now accurately reflects both the temporary false closure and its resolution. STEP-515 is fully closed at the same authorization boundary.

### STEP-516 clarify `run_command` versus `start_process`

**Status / goal:** Completed and reviewed. Implement the next bounded P1 tool-selection slice: make the model-facing process descriptions separate finite commands that are expected to exit from persistent or interactive commands. Do not change tool names, projections, schemas, handlers, process lifecycle, authorization, Policy, workspace/OAuth authority, or any deployment/runtime state.

**Files changed / implementation:** Updated `src/server.ts`, `test/phase-4a-integration.test.mjs`, `test/phase-5-v4-inherited-contract.test.mjs`, `test/phase-7-v5-runtime-inheritance.test.mjs`, `Memory.md`, and this Part 10 entry. V3–V5 now describe `run_command` as finite/expected-to-exit only and explicitly forbid using it as a persistent-process fallback. The text tells standard-mode callers that `start_process` exists only in full tool mode. Full-mode `start_process` covers dev servers/watchers and interactive REPLs, Windows ConPTY plus `write_process_input`, incremental `read_process_output` calls that carry each non-null `next_cursor`, owned-process termination, configured lifetime, and truthful `full_access` ambient-authority/no-sandbox limits. V4/V5 retain exact-candidate verification wording. No exposure, schema, handler, lifecycle, cleanup, or security behavior changed.

**TDD / verification:** The first direct V3/V4 description contracts produced the intended `8/10` RED against both old descriptions, then passed `10/10` after the initial source edit. The first seven-file compatibility run passed `46/47`; its only failure was a new-test mistake that assumed V5 standard exposed `start_process`. Correcting that test to freeze standard `run_command` plus absent `start_process`, and full-mode `start_process`, produced `47/47`. Initial process smoke passed `18/18`; TypeScript build, repository policy, diff check, and scoped credential scan passed. Adversarial review then found a real product-guidance defect in the initial wording: standard mode was told to call a full-only tool. It also identified misleading execution-authority language and incomplete interactive/cursor guidance. Review-driven tests produced `9/13` with four expected description REDs before the final source correction. Frozen CodexPro Node `v24.15.0` then ran TypeScript build, the combined V3/V4/V5/process/production focused suite at `65/65`, and repository policy at PASS. V1 wire snapshot coverage passed unchanged because V1 does not expose these V3 process tools; no V3+ descriptor hash constant exists.

**Review / risks / rollback / next:** Three independent post-correction reviews found no remaining P0–P3 and verified standard/full projection truthfulness, direct/supertool compatibility, ConPTY/input/cursor/termination facts, and the unchanged security boundary. This description change does not prove lower wrong/redundant calls, persistent-process success, or cleanup behavior beyond existing tests; no ChatGPT Web benchmark or App refresh was run. Cached Apps require their separate Scan Tools refresh or recreation before users receive new descriptors. `read_process_output.wait_ms` remains outside this slice: the description promises cursor-based incremental reads but does not promise blocking wait behavior. Rollback only the four STEP-516 source/test hunks and correct `Memory.md` in place; preserve this append-only record and append any factual correction. No staging, commit, push, publication, credential, OAuth/DNS/Tunnel, retained runtime, or target mutation occurred. Stop at STEP-516; App refresh/trace recollection, another P1 slice, and later phases require a separate owner decision.

### STEP-517 make `read_process_output.wait_ms` real and cancellable

**Status / goal:** Completed and reviewed under the owner's explicit follow-up authorization. Wire the already-published `wait_ms: 0..30000` input to bounded waiting for owned running persistent processes, without redesigning the process subsystem or changing tool names, counts, schemas, scopes, execution authority, workspace/OAuth ownership, deployment, or public runtime state. Preserve immediate reads for retained `run_command` terminal records and for persistent records already exited, failed, or terminated.

**Files changed / implementation:** The STEP-517 logical delta updates `src/process/outputRing.ts`, `src/process/processManager.ts`, `src/productionRuntime.ts`, `src/server.ts`, `test/process-output-ring.test.mjs`, `test/process-manager.test.mjs`, `test/phase-4a-integration.test.mjs`, `test/phase-5-v4-inherited-contract.test.mjs`, `Memory.md`, and this Part 10 entry. `OutputRing.waitForChange()` now owns a bounded timeout and one cleanup path for append, close, explicit state notification, timeout, and abort. `ProcessManagerV3.readResult()` first returns any unread page; only a running empty non-EOF page waits, then rechecks exact record/context ownership and resumes from `page.next` before projecting output. Termination notifies waiters before host cleanup settles; natural exit closes the ring; timeout returns the current empty page; zero wait and all terminal records return immediately. Production routing now awaits this method for persistent IDs while the `RunCommandRuntimeV3` retained-terminal path remains immediate. Exact V3/V4+ descriptions explain cursor reuse, the full-mode-only persistent-process boundary, the thirty-second ceiling, and immediate exited/failed/terminated behavior.

**Cancellation and security review correction:** The first completed implementation used the existing `OutputRing` abort seam but did not receive the MCP SDK cancellation signal. Review correctly identified that cancelled HTTP requests could otherwise leave background waiters until timeout, so the optional handler extra was threaded through direct, Policy, OAuth, mutation, validation, supertool, and production paths. A second adversarial pass then found that forwarding the raw SDK extra would newly expose `authInfo.token`, session/request metadata, and transport request/notification capabilities to dependency handlers. The final implementation projects the SDK extra at the outer wrapper into a new frozen `{ signal }` object and passes only that minimal view. Tests inject a fake raw extra and prove both direct and supertool child handlers receive a different frozen object containing only the identical `AbortSignal`; manager cancellation removes the pending waiter/timer and a zero-wait follow-up returns normally.

**TDD and exact verification:** The initial focused RED exposed both missing behavior seams: `ProcessManagerV3.readResult` did not exist and the new ring timeout case remained pending. After the minimal wait implementation, the first four-file process/runtime set passed 38/38. A V4 description test briefly read stale `dist` output because the build had not yet run; a fresh build fixed the test without a source correction. Post-review fixes removed wall-clock threshold assertions, clarified standard/full wording, propagated cancellation, and closed the raw-extra security leak. The final local command `npm run test:focused -- test/process-output-ring.test.mjs test/process-manager.test.mjs test/run-command-contract.test.mjs test/phase-4a-integration.test.mjs test/phase-5-v4-inherited-contract.test.mjs test/phase-7-v5-runtime-inheritance.test.mjs test/production-runtime-integration.test.mjs test/policy-integration.test.mjs test/policy-v3-approval-integration.test.mjs test/codexgpt-contract.test.mjs test/mutation-architecture.test.mjs` passed 80/80. Final managed-major `node scripts/toolchain-manager.mjs matrix --major all --root C:\Users\Administrator\AppData\Local\CodexPro\toolchains -- node --test test/process-output-ring.test.mjs test/process-manager.test.mjs test/run-command-contract.test.mjs test/phase-4a-integration.test.mjs test/phase-5-v4-inherited-contract.test.mjs test/phase-7-v5-runtime-inheritance.test.mjs` passed 38/38 on Node 20.20.2 and 38/38 on Node 24.15.0; the same managed-major wrapper running `npm run build` passed on both. `npm run policy:check`, `git diff --check` apart from informational LF-to-CRLF warnings, and the scoped credential-value scan passed.

**Smoke / failed attempts:** Detached run `2026-08-24T07-38-05-451Z-step517-process-smoke-r3-f2945fa8` executed `node scripts/run-smoke.mjs`, exited 0 with complete untruncated stdout, zero stderr, cleaned temporary state, and passed analysis, analysis CLI, main, HTTP, pro CLI, doctor, settings, and all handoff smoke domains. Two earlier detached launch shapes produced environment/invocation failures only and are not counted as product evidence: run `2026-08-24T07-30-29-492Z-step517-process-smoke-74180a11` could not spawn bare `npm` (`ENOENT`), and run `2026-08-24T07-33-57-784Z-step517-process-smoke-r2-b8165fc5` could not spawn `npm.cmd` (`EINVAL`). Retrying the package's underlying Node script was the exact Windows-compatible form; no product file or external state changed in either failed attempt.

**Review / risks / rollback / next:** Three post-result reviewers found and then closed the standard-mode wording, cancellation, timing-test, handler-evidence, and raw-extra findings; final code review found no remaining P0–P3. One independent P1 was discovered and verified against `HEAD` as pre-existing rather than introduced here: natural-exit verification/audit work has no joinable record promise, so `revokeAll()`/`close()` can return before it settles and a verification receipt can be published afterward. It is now an explicit index limitation and the strongest candidate for a separately authorized lifecycle/security hardening step before any stronger revocation or publication claim; it is not silently expanded into STEP-517. Rollback only the STEP-517 hunks listed above, preserving the earlier STEP-516 description work and append-only history; correct `Memory.md` in place and append any archive correction. No staging, commit, push, publication, App refresh, benchmark, credential, OAuth/DNS/Tunnel, retained target, or public runtime mutation occurred. Stop at STEP-517; lifecycle hardening, another P1 slice, App refresh/trace recollection, and later phases require a separate owner decision.

### STEP-518 make persistent-process finalization joinable and revocation-safe

**Status / goal:** Completed and adversarially reviewed under the owner's explicit next-step authorization. Close the pre-existing race recorded by STEP-517: natural-exit verification/audit must be represented by joinable lifecycle work, `revokeAll()`/`close()` must not return while that work is still capable of publishing a new verification receipt, and root drain must not treat a finalizing process as finished. Preserve V1–V5 tool counts, schemas, scopes, full-access authority, workspace/OAuth ownership, and all deployment/public runtime boundaries.

**Files changed / implementation:** The STEP-518 logical delta updates `src/process/outputQuota.ts`, `src/process/processManager.ts`, `src/server.ts`, `test/process-manager.test.mjs`, `test/persistent-verification-receipt.test.mjs`, `test/phase-4a-integration.test.mjs`, `test/phase-5-v4-inherited-contract.test.mjs`, `Memory.md`, and this Part 10 entry. `ProcessManagerV3` now records a joinable natural-exit lifecycle plus any lifecycle failure, tracks in-flight starts, rejects starts while revoking/closing or after close, and uses a revocation generation so a start blocked in pre-execution verification cannot land after revocation. Revocation invalidates every snapshot record before any await, synchronously starts all required running-process terminations, and then joins lifecycle, termination, and admitted-start promises together; close is idempotent, waits through the same path, and cleans records/cursor in `finally`. Root activity and drain include pending natural-exit and termination cleanup. Errors remain contained from the backend callback but are rethrown to revoke/close/drain joiners instead of reporting false success.

**Finalization / receipt / quota contract:** Host exit or termination state and exit code become visible immediately and wake an existing reader, but the output ring remains `eof=false` while verification, durable terminal audit, cleanup, and terminal retention are finalizing. With positive `wait_ms` and no unread output, a follow-up read can wait for this finalization; `eof=true` never waits. On success, terminal quota reservation, receipt issuance, `retentionReady`, and ring close occur in one no-await section, so the first final EOF read sees the final receipt or a truthful null. On lifecycle failure the record changes to `failed` before EOF closes. Pending cleanup cannot be timed out or evicted from the manager, and finalizing work does not consume terminal-retention quota. `OutputQuotaManager` now returns a structural `OUTPUT_TERMINAL_QUOTA_EXCEEDED` error with `session|server` scope: session pressure evicts only the same context's settled `retentionReady` record, while server pressure may select globally. This preserves foreign-context retained output/receipts and avoids `instanceof` drift across TS loader instances.

**TDD / exact verification:** The initial `npm run test:focused -- test/process-manager.test.mjs` RED passed 15/17 and failed exactly the two target assertions: revoke returned before natural-exit verification settled, and root activity returned false during that work. After the minimal join/invalidator, the process/receipt pair passed 20/20. Post-result review drove further deterministic regressions for multi-record revoke ordering, verification-stage start versus close/revoke, repeated close, lifecycle error propagation, state/EOF/receipt finalization, root-drain follow-up reads, pending cleanup under terminal pressure, synchronous cap-one exits, and cross-context session quota eviction. Final local `npm run test:focused -- test/process-output-ring.test.mjs test/process-output-quota.test.mjs test/process-manager.test.mjs test/run-command-contract.test.mjs test/phase-4a-integration.test.mjs test/phase-5-v4-inherited-contract.test.mjs test/phase-7-v5-runtime-inheritance.test.mjs test/production-runtime-integration.test.mjs test/policy-integration.test.mjs test/policy-v3-approval-integration.test.mjs test/codexgpt-contract.test.mjs test/mutation-architecture.test.mjs test/persistent-verification-receipt.test.mjs` passed 94/94 after a fresh TypeScript build. Managed-major `node scripts/toolchain-manager.mjs matrix --major all --root C:\Users\Administrator\AppData\Local\CodexPro\toolchains -- node --test test/process-output-ring.test.mjs test/process-output-quota.test.mjs test/process-manager.test.mjs test/run-command-contract.test.mjs test/phase-4a-integration.test.mjs test/phase-5-v4-inherited-contract.test.mjs test/phase-7-v5-runtime-inheritance.test.mjs test/persistent-verification-receipt.test.mjs` passed 52/52 on Node 20.20.2 and 52/52 on Node 24.15.0; the same managed wrapper running `npm run build` passed on each major. `npm run policy:check` passed; `git diff --check` reported only informational LF-to-CRLF warnings. The scoped credential-value scan found no unexpected value; its sole broad-pattern match was the intentional test sentinel `test-token-must-not-reach-handler`, which proves raw SDK auth data is not forwarded.

**Smoke / review / risks / rollback / next:** Final detached run `2026-08-24T08-44-08-173Z-step518-lifecycle-smoke-final-e65beaf5` executed `node scripts/run-smoke.mjs`, exited 0, cleaned temporary state, retained complete untruncated stdout, produced zero stderr, and passed analysis, analysis CLI, main, HTTP, pro CLI, doctor, settings, and all handoff smoke domains. Three post-result reviewers repeatedly rechecked each correction and ended with no remaining P0–P3. A truly nonsettling downstream verification/audit promise will now keep revoke/close pending rather than manufacture false completion; this step adds no independent cancellation/deadline to those existing local operations. Revocation blocks future receipt publication but does not erase an already published receipt; durable validation/invalidation policy is a separate boundary. Roll back only the STEP-518 hunks named above, preserve STEP-516/517 and append-only history, correct `Memory.md` in place, and append any archival correction. No staging, commit, push, publication, App refresh, benchmark, credential, OAuth/DNS/Tunnel, retained target, deployment, or public runtime mutation occurred. Stop at STEP-518; another P1 slice, App refresh/trace recollection, and later gated work require a separate owner decision.

### Part 10 volume closure

Part 10 is sealed after STEP-518. Its byte size remains below 48 KiB, but a complete STEP-519 record would cross the direct-read ceiling; STEP-519 therefore starts in Part 11. Preserve this volume unchanged and append any later factual correction to the active continuation.
