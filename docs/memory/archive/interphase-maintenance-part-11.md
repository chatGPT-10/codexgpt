# Interphase Maintenance Archive — Part 11

Continuation of the append-only interphase record after Part 10 was sealed at STEP-518. This volume begins with STEP-519 and remains active until the repository memory protocol closes it.

## 2026-08-24

### STEP-519 establish the explainable-configuration foundation

**Status / goal:** Completed and adversarially reviewed under the owner's instruction to improve the product from the approved plan without another ChatGPT Web validation or trace rerun. Implement the first narrow Work Package C slice: a pure source/provenance resolver and Phase 8 `auth.mode` adoption. Preserve all current defaults, network/permission/auth behavior, profile values, public tool/schema counts, and deployment state. Do not claim the later top-level argv/env/profile/cwd loader, strict profile schema, `--no-profile` resolver boundary, or doctor/start fingerprint parity.

**Files changed / implementation:** Added `src/configResolver.ts` and `test/config-resolver.test.mjs`; updated `src/auth/configuration.ts`, `src/config.ts`, `test/phase-8-auth-config.test.mjs`, `Memory.md`, and this new Part 11 archive. The pure resolver represents CLI, current-process/persisted-user environment, canonical-profile, default, and compatibility origins; enforces exact layer/origin pairing and same-layer uniqueness; returns structured stable error codes, cloned origins, safe remediation, compatibility diagnostics, and restart metadata; preserves Windows case-insensitive environment lookup while treating empty input as present. `ResolvedValue` and `ResolvedConfig` are immutable owned snapshots. A module-private `WeakSet` prevents structurally or reflectively forged values/diagnostics from entering aggregation. Secret parser causes are discarded, and both direct and aggregate JSON/Node inspection projections expose only `set|missing`. Fingerprints are SHA-256 over a typed, code-unit-sorted, redacted encoding derived from the same complete field set as `effective`; semantic keys and effective property names are both bound, and unsupported/non-finite/cyclic values fail with their exact source. Phase 8 `auth.mode` now consumes this model and exposes exact environment/profile origin plus safe `legacy|oauth` remediation, while the existing adapter preserves its legacy precedence, source labels, default, `AUTH_MODE_INVALID` code, and invalid-profile user text.

**TDD / review corrections:** The first RED failed because `src/configResolver.ts` did not exist; after the initial implementation the new suite passed 6/6. A second RED added structured `auth.mode` origin and initially failed because the API did not exist. Post-result three-way review found real gaps: secret-bearing parser causes, independent effective/fingerprint inputs, mutable origins/Map, layer/source spoofing, ambiguous canonical encoding, unsafe direct secret inspection, mutable/forged diagnostics, generic auth remediation, and a reflectively copyable Symbol brand. Each finding received a deterministic regression before correction. The final ownership boundary uses a private `WeakSet`, and the test copies every own key/symbol/descriptor from a legitimate result yet remains rejected. Final independent contract and UX reviews found no remaining P0–P3; the security reviewer supplied the WeakSet correction, and the contract reviewer independently confirmed it after implementation.

**Exact verification:** `npm run test:focused -- test/config-resolver.test.mjs test/phase-8-auth-config.test.mjs` passed 24/24 and `npm run build` passed. The broader focused command covering the new resolver, Phase 8 auth config/doctor, doctor shell/profile behavior, realpath config, policy profile store, Phase 3D config, and server config passed 51 tests with one established platform-capability skip and zero failures. Managed `node scripts/toolchain-manager.mjs matrix --major all --root C:\Users\Administrator\AppData\Local\CodexPro\toolchains -- node --test test/config-resolver.test.mjs test/phase-8-auth-config.test.mjs test/phase-8-auth-doctor.test.mjs test/doctor-shell.test.mjs test/config-realpath.test.mjs` passed 33/33 on Node 20.20.2 and 33/33 on Node 24.15.0; the same managed-major wrapper running `npm run build` passed on both. `npm run policy:check`, `git diff --check` apart from informational existing LF-to-CRLF warnings, and the scoped credential-pattern scan passed.

**Smoke / risks / rollback / next:** Detached owned run `2026-08-24T09-50-17-183Z-step519-config-smoke-e6b80e5f` executed `node scripts/run-smoke.mjs`, exited 0, cleaned temporary state, retained complete untruncated stdout, produced zero stderr, and passed analysis, analysis CLI, main, HTTP, pro CLI, doctor, settings, and all handoff smoke domains. This slice creates a trustworthy shared value/source/fingerprint primitive but does not yet make doctor and start consume one complete raw-input resolver, does not validate every unknown/range/path/combination profile field, and does not expose `config explain`; those remain the next bounded configuration steps. Roll back only the STEP-519 files/hunks named above, preserving STEP-516–518 and append-only Part 10; correct `Memory.md` in place and append any historical correction here. No staging, commit, push, publication, App refresh, benchmark, credential, OAuth/DNS/Tunnel, retained target, service, deployment, or public runtime state changed.

### STEP-520 make the bootstrap configuration input explicit

**Status / goal:** Completed and adversarially reviewed under the owner's instruction to continue plan-based improvement without another ChatGPT Web validation or trace rerun. Implement one bounded follow-up to STEP-519: resolve canonical-root input and profile bypass from explicit argv/environment/cwd/platform inputs, then make `loadConfig` consume those inputs without ambient environment or cwd leakage. Preserve effective defaults, OAuth explicit-root semantics, public tool/schema counts, network/permission authority, and deployment state. Doctor/start complete-fingerprint parity and strict profile schema remain separate steps.

**Files changed / implementation:** Updated `src/configResolver.ts`, `src/config.ts`, `src/profileStore.ts`, `scripts/codexgpt.mjs`, `test/config-resolver.test.mjs`, `test/phase-8-auth-config.test.mjs`, `Memory.md`, and this archive. `resolveConfigBootstrap` now resolves `--root` over canonical `CODEXGPT_ROOT`, compatibility `CODEBASE_BRIDGE_REPO_ROOT`, then explicit cwd, and includes an immutable fingerprinted `profile.disabled` decision. Both `--no-profile=value` and `--no-profile value` fail with structured remediation; a bare flag skips supplied and disk profiles before access. `loadConfig` accepts explicit environment/cwd/config-platform/filesystem-platform/profile-loader inputs; relative root, allowed-root, Codex directory, worktree root, and relative `CODEXGPT_HOME` paths use the supplied cwd. Windows environment access is case-insensitive and lazy: a conflict fails only when that key is consumed, so shadowed root or unrelated casing duplicates cannot block startup. Host filesystem syntax is separate from simulated configuration-platform semantics; Windows cwd accepts only drive-qualified local paths and rejects root-relative, drive-relative, UNC, and device forms with actionable text. Default profile reads receive the same explicit environment/cwd. The CLI's internal mutation config replaces its already-parsed `--root` instead of appending a duplicate.

**TDD / adversarial corrections:** Initial RED produced five expected failures for the absent bootstrap export, missing profile bypass, and ambient environment leakage. Three independent post-result reviews then reproduced shadowed invalid-root parsing, Windows case-fold gaps, relative-root authority drift, ambient `CODEXGPT_HOME`, split-form profile-bypass assignment, weak loader/cwd tests, empty-cwd home expansion, eager unrelated-environment conflicts, coupled profile fixtures, and target-platform/host-filesystem conflation. Each received a deterministic regression before correction. The first detached smoke run `2026-08-24T10-34-30-909Z-step520-config-bootstrap-smoke-2e91b9a2` materially failed after seven passed domains because `commitWorkspaceWrites` appended a second internal `--root`; `replaceSingleValueOption` corrected that internal call without weakening public duplicate-source rejection. Final contract, security/scope, and UX reviews reported no remaining P0–P2.

**Exact verification:** Final narrow `npm run test:focused -- test/config-resolver.test.mjs test/phase-8-auth-config.test.mjs` passed 38/38 and `npm run build` passed. The broader focused command covering resolver/auth config and doctor, doctor shell/profile behavior, realpath, server config, Phase 3D config, policy profile store, transaction config, and path policy passed 67/68 with one established platform-capability skip and zero failures. Managed `node scripts/toolchain-manager.mjs matrix --major all --root C:\Users\Administrator\AppData\Local\CodexPro\toolchains -- node --test test/config-resolver.test.mjs test/phase-8-auth-config.test.mjs test/phase-8-auth-doctor.test.mjs test/doctor-shell.test.mjs test/config-realpath.test.mjs` passed 47/47 on Node 20.20.2 and 47/47 on Node 24.15.0; the same managed-major wrapper running `npm run build` passed on both. `npm run policy:check`, `git diff --check` apart from informational existing LF-to-CRLF warnings, and the STEP-520 added-line credential-pattern scan passed.

**Smoke / risks / rollback / next:** Final detached owned run `2026-08-24T10-38-53-306Z-step520-config-bootstrap-smoke-final-355a7bea` executed `node scripts/run-smoke.mjs`, exited 0, cleaned temporary state, retained complete untruncated 276-byte stdout, produced zero stderr, and passed analysis, analysis CLI, main, HTTP, pro CLI, doctor, settings, and execute/watch/loop handoff smoke domains. The bootstrap fingerprint intentionally covers only root input and profile bypass; `loadConfig` still returns the established runtime object, and doctor/start do not yet compare one complete resolved fingerprint. Strict unknown/range/path/combination profile validation also remains later. Roll back only the STEP-520 hunks/files named above, retaining STEP-519's resolver foundation and append-only history; correct `Memory.md` in place and append any historical correction here. The next approved local slice is doctor/start complete-fingerprint parity and mismatch reporting. No staging, commit, push, publication, App refresh, benchmark, credential, OAuth/DNS/Tunnel, retained target, service, deployment, or public runtime state changed.

## 2026-08-27

### STEP-521 consume one complete effective configuration snapshot

**Status / goal:** Completed under the owner's instruction to continue the approved local improvement plan without another ChatGPT Web benchmark rerun. Make doctor, start, and the HTTP child consume one complete immutable configuration snapshot; expose one secret-redacted public fingerprint for diagnosis, use a separate keyed full-value proof for launcher-to-server integrity, and report only meaningful live-runtime mismatches. Preserve auth modes, tool counts/schemas, authority, network defaults, profiles, and deployment state.

**Files changed / implementation:** Updated `src/config.ts`, `src/http.ts`, `src/http/oauthMcpRuntime.ts`, `src/productionRuntime.ts`, `src/git/productionBootstrap.ts`, `src/server.ts`, `src/profileStore.ts`, `scripts/codexgpt.mjs`, `scripts/codexgpt-entry.mjs`; added `test/config-fingerprint.test.mjs` and `test/doctor-start-config-parity.test.mjs`; updated `Memory.md` and this archive. `loadResolvedConfig` now freezes the full effective config including resolved OAuth deployment, transaction-state root availability, and request/tool logging flags. Its JSON and Node-inspection projections redact `authToken` to `set|missing`; the public SHA-256 fingerprint is safe to display. A transient 256-bit launcher key produces an HMAC-SHA-256 proof over the exact unredacted snapshot, so different token values cannot pass the child handshake; `--print-env` redacts the key. HTTP, OAuth MCP, Git bootstrap, persistent runtime, and tool-call logging consume the snapshot rather than rereading those ambient inputs after the assertion. Explicit Windows environments without `CODEXGPT_HOME`/`LOCALAPPDATA` record `transactionStateRoot=null`; only a capability that actually needs durable state fails closed with one restart action.

**TDD / review corrections:** Initial RED covered public parity, mismatch, and HTTP pre-listen rejection. Post-result contract/security/UX review reproduced two token values sharing the public fingerprint, omitted OAuth/state/logging fields, post-assertion ambient rereads, `util.inspect` token leakage, duplicate/valued bootstrap argv being folded, forged/stale runtime records, normal live-runtime port false failures, and copyable unquoted/incomplete restart commands. Corrections split public diagnosis from exact keyed integrity, added safe inspection, preserved raw argv for strict bootstrap validation, validated runtime record version/root/PID/fingerprint before comparison, treated a matching live recorded endpoint as active, generated PowerShell-safe public-setting restart commands, and kept no-runtime comparison neutral. A final regression caught and fixed the transient integrity key appearing in `--print-env`.

**Exact verification:** `npm run test:focused -- test/config-fingerprint.test.mjs test/doctor-start-config-parity.test.mjs` passed 11/11. The broader focused configuration/OAuth/doctor/server/transaction command passed 72/72. The retained CodexPro Node 20.20.2 and 24.15.0 executables each passed the same 72-test set; `scripts/toolchain-manager.mjs matrix --major all` independently passed the final STEP-521 11/11 on each major, and managed builds passed on both. `npm run build`, `npm run policy:check`, `git diff --check` apart from existing informational LF-to-CRLF warnings, and the scoped credential-pattern scan passed.

**Smoke / risks / rollback / next:** Detached owned run `2026-08-27T06-02-50-346Z-step521-config-fingerprint-smoke-8698502e` ran the 11 STEP-521 tests, exited 0, cleaned temporary state, retained complete untruncated stdout, and produced zero stderr. The public fingerprint intentionally proves secret state, not secret equality; only the non-public HMAC launch proof checks exact values. Runtime records remain same-user operational evidence, not an OS security boundary. Strict unknown/range/path/combination profile schema validation remains the next bounded configuration slice. Roll back only STEP-521 hunks/tests and its `Memory.md` summary while retaining STEP-519/520; append any historical correction here. No staging, commit, push, publication, App refresh, benchmark, credential, DNS/Tunnel, service, deployment, or public runtime state changed.

### STEP-522 reject invalid saved workspace profiles before use

**Status / goal:** Completed under the owner's instruction to continue the local improvement plan without another ChatGPT Web rerun. Add one shared strict schema boundary to every saved workspace-profile read/write path used by the public CLI and runtime loader. Preserve reviewed v1/v2 profile compatibility, auth/OAuth semantics, effective defaults, tool counts/schemas, authority, network behavior, and deployment state; do not claim crash-safe profile migration or atomic backup yet.

**Files changed / implementation:** Added `scripts/workspace-profile-schema.mjs`, its TypeScript declaration, and `test/workspace-profile-schema.test.mjs`; updated `src/profileStore.ts`, `scripts/codexgpt.mjs`, `scripts/test-execution-profile-manifest.mjs`, `test/phase-8-auth-cli.test.mjs`, `Memory.md`, and this archive. One dependency-free shared validator now covers public CLI load/list/save and the TypeScript profile store. It accepts reviewed schema v1/v2 documents, normalizes new writes to v2, and fails before profile consumption or file creation on malformed JSON, non-object documents, future/invalid versions, missing or mismatched canonical roots, unknown top-level/nested fields, wrong scalar types, invalid enum/range/identifier/HTTPS-origin/absolute-path values, incomplete tunnel/Bash/OAuth combinations, or equal OAuth public/admin ports. Unknown fields receive a bounded nearest-field suggestion. Errors expose stable code, exact JSON path, escaped profile location, and one edit-or-reset remediation without echoing field values or secrets. Existing OAuth error codes remain compatible.

**TDD / corrections:** Initial RED passed only the legacy-compatible fixture and failed four strict-schema/CLI cases. The first GREEN centralized schema use and made those cases pass; compatibility regression then exposed one pre-existing STEP-521 source-contract assertion that no longer represented the child `--no-profile` propagation, so the test now binds both explicit `--root` and that bootstrap flag. The repository execution-profile audit also exposed that STEP-519–521's three new tests had never been added to the fail-closed Windows inventory; STEP-522 added all four missing configuration tests and the inventory/domain gates passed. Final review tightened ISO UTC timestamps, accepted the two equivalent HTTPS-origin spellings, prevented invalid saves from creating the profile directory, escaped diagnostic paths, added nearest-field suggestions, and confirmed package inclusion of the shared `.mjs`/`.d.mts` files. The first passing smoke used the repository root as its evidence root by mistake; its exact generated files were removed, then the same command was rerun successfully under the ignored `.ai-bridge/runs` boundary.

**Exact verification:** Final `npm run build` and the broad focused configuration/profile/OAuth/settings/doctor/package/inventory command passed 103/103. `node scripts/toolchain-manager.mjs matrix --major all --root %LOCALAPPDATA%\CodexPro\toolchains -- node --test ...` passed 57/57 on Node 20.20.2 and 57/57 on Node 24.15.0; managed TypeScript builds passed on both majors. `npm run policy:check`, `git diff --check` apart from informational existing LF-to-CRLF warnings, package-contents validation, and the scoped credential-pattern scan passed.

**Smoke / risks / rollback / next:** Detached owned run `2026-08-27T09-45-13-517Z-step522-profile-schema-smoke-final-768f0502` ran the final profile/fingerprint/doctor contracts from ignored evidence state, exited 0, cleaned temporary state, retained all 1668 stdout bytes without truncation, and produced zero stderr. Schema validation prevents malformed or future profiles from being consumed, but profile writes still use the established direct file replacement and therefore do not yet claim candidate migration, backup retention, failure-injection recovery, or old-binary overwrite protection for a later schema. The next bounded slice is version-aware atomic profile persistence/migration with backup and failure-injection tests; `config explain` follows separately. Roll back the STEP-522 schema imports/files/tests/inventory/doc hunks while retaining STEP-519–521, then append any historical correction here. No staging, commit, push, publication, App refresh, benchmark, credential, OAuth/DNS/Tunnel, service, deployment, or public runtime state changed.

### STEP-523 make saved-profile migration atomic and recoverable

**Status / goal:** Completed under the owner's instruction to execute the next approved local improvement without another ChatGPT Web benchmark rerun. Replace duplicated direct profile writes with one version-aware persistence boundary. Keep reads side-effect free; on an explicit save, refuse invalid/current-future state before mutation, preserve an exact independent v1 recovery copy, atomically publish v2, and make interrupted migration retryable. Preserve profile schema, runtime defaults, OAuth semantics, tool counts/authority, network behavior, and deployment state.

**Files changed / implementation:** Added `scripts/workspace-profile-persistence.mjs`, its TypeScript declaration, and `test/workspace-profile-persistence.test.mjs`; updated `src/profileStore.ts`, `scripts/codexgpt.mjs`, `scripts/test-execution-profile-manifest.mjs`, `test/mutation-architecture.test.mjs`, `Memory.md`, and this archive. Public CLI and runtime saves now call one shared persistence function. Candidate and existing documents are validated before directory creation. Existing malformed, root-mismatched, or future-version profiles cannot be overwritten. A v1 save first creates the exclusive independent `profiles/backups/<profile-id>.v1.json`, verifies exact bytes and a distinct file identity, flushes it, then uses the existing staged-file/fsync/atomic-rename helper to publish v2. A stable backup is never overwritten; an exact existing backup supports retry, while a conflicting backup fails closed. Immediately before rename, file identity and bytes must still match the inspected primary. Explicit `settings delete` removes only that profile and its exact migration backups while preserving other workspaces.

**TDD / corrections:** Initial RED failed because the shared persistence module did not exist. The first implementation made ten of eleven schema/persistence checks pass but exposed that an interrupted hard-link backup left the primary with link count two and blocked retry. Recovery was first taught that exact state; a deeper review then proved the hard link was not an independent backup before replacement, because an in-place edit would change both names. A new RED required distinct identities, and the implementation switched to exclusive independent copy creation. Native Windows then returned `EPERM` when flushing a read-only backup handle; opening the already-created file as `r+` without changing bytes made the flush portable and remained covered by exact post-copy content/identity checks. The fail-closed mutation inventory was updated for the centralized profile writer and obsolete direct `src/profileStore.ts` mutations were removed.

**Exact verification:** `npm run build` passed. The broad configuration/profile/OAuth/settings/doctor/atomic-file/package/inventory command passed 114/114. `node scripts/toolchain-manager.mjs matrix --major all --root %LOCALAPPDATA%\CodexPro\toolchains -- node --test test/workspace-profile-persistence.test.mjs test/workspace-profile-schema.test.mjs test/phase-8-auth-config.test.mjs test/phase-8-auth-cli.test.mjs` passed 54/54 on Node 20.20.2 and 54/54 on Node 24.15.0; managed TypeScript builds passed on both majors. The persistence suite proves exact independent backup bytes, retry after post-backup failure, old-primary survival on rename failure, future/malformed/root-mismatched refusal, conflicting-backup refusal, pre-commit conflict detection, scoped deletion, and the supported public-entry flow.

**Smoke / risks / rollback / next:** Detached owned run `2026-08-27T16-04-10-888Z-step523-profile-persistence-smoke-17fae626` ran profile persistence/schema plus fingerprint/doctor contracts from ignored evidence state, exited 0, cleaned temporary state, retained all 2455 stdout bytes without truncation, and produced zero stderr. Atomic rename and flushed independent backup handle ordinary failures and process interruption, but do not claim absolute power-loss durability or an OS-wide concurrent-writer lock; the identity/byte precondition narrows, but cannot eliminate, the final check-to-rename race. Roll back the STEP-523 shared persistence files/imports/tests/inventory/docs while retaining STEP-519–522. The next bounded slice is user-facing secret-redacted `config explain`; complete-trace recollection and App refresh remain separate. No staging, commit, push, publication, App refresh, benchmark, credential, OAuth/DNS/Tunnel, service, deployment, or public runtime state changed.

### STEP-524 expose read-only secret-redacted configuration explanation

**Status / goal:** Completed under the owner's instruction to execute the next plan-based improvement without another ChatGPT Web benchmark rerun. Add `codexgpt config explain [public-key] [--json]` through the supported public entry. It must reuse the exact start planner, remain read-only, show why a public input won and which lower-priority sources it overrode, never reveal a current or overridden secret value, expose one complete machine-readable effective snapshot, and return before profile creation, port probing, integrity-key creation, server/tunnel startup, or external mutation.

**Files changed / implementation:** Added `scripts/config-explain.mjs` and `test/config-explain.test.mjs`; updated `scripts/codexgpt.mjs`, `scripts/http-smoke.mjs`, `scripts/test-execution-profile-manifest.mjs`, `test/public-cli-help.test.mjs`, `test/workspace-profile-persistence.test.mjs`, `README.md`, `README_ZH.md`, `Memory.md`, and this archive. The public command supports the complete public-input view or one exact key such as `auth.mode`; text shows effective value, selected source, lower-priority sources, restart timing, and a secret-safe foreground restart command. JSON schema v1 carries the public fingerprint, every effective runtime key with absent values represented as `null`, structured public-input explanations, and the next action. Secret values are only `set|missing`; source objects contain names/paths but never candidate values. Raw argv is now used only for strict bootstrap validation, while the final launcher snapshot resolves from canonical planned environment plus canonical root/profile flag. This preserves duplicate-root and valued-flag failures while preventing relative `--codex-dir` from being resolved once against the workspace and again against the launcher directory. Early server-start failures append the already-redacted child log tail instead of returning an opaque health timeout. Help and both active READMEs document the read-only boundary.

**TDD / corrections:** Initial RED had 3/3 failures because `config explain` was routed as an unknown settings action. The first GREEN supported full text/JSON output; a second RED added optional `auth.mode`, unknown-key refusal, restart guidance, complete absent-key JSON representation, and the literal secret value `missing` still projecting as `set`. Broad verification then exposed two prior reliability defects: STEP-523 compared a sorted random SHA filename list with an unsorted expectation, and HTTP smoke seeded a now-invalid `cloudflare-named` v1 fixture without `tunnelName`; both fixtures were corrected without weakening production validation. The first full smoke failed on the invalid fixture. The second failed with an opaque CLI health timeout; bounded smoke diagnostics plus a focused RED proved launcher/server fingerprint drift for relative `--codex-dir` when root differed from the repository. Bootstrap-only raw-argv validation plus one canonical planned snapshot repaired the production cause. The smoke harness also fixed its already-exited child wait race and preserves bounded child diagnostics. Targeted `http-smoke-compat` then passed, and the final full smoke passed.

**Exact verification:** `npm run test:focused -- test/config-explain.test.mjs test/config-fingerprint.test.mjs test/config-resolver.test.mjs test/doctor-start-config-parity.test.mjs test/config-realpath.test.mjs test/workspace-profile-schema.test.mjs test/workspace-profile-persistence.test.mjs test/phase-8-auth-config.test.mjs test/phase-8-auth-cli.test.mjs test/public-cli-help.test.mjs test/cli-hostname-propagation.test.mjs test/server-config-contract.test.mjs test/package-contents.test.mjs test/mutation-architecture.test.mjs` passed 107/107. Managed Node 20.20.2 and 24.15.0 each passed 43/43 across explain/fingerprint/resolver/doctor/public-entry/profile-persistence tests; managed `npm run build` passed on both. `npm run task:run -- -- node scripts/http-smoke-compat.mjs` passed. Detached owned run `2026-08-27T17-07-15-379Z-step524-config-explain-smoke-185b3be6` executed all eight `run-smoke.mjs` domains, exited 0, cleaned temporary state, retained 276 untruncated stdout bytes, and produced zero stderr. `npm run policy:check`, `git diff --check` apart from informational LF-to-CRLF warnings, and the scoped secret-pattern scan passed. The active archive remains below its 48 KB continuation threshold.

**Risks / rollback / next:** The complete runtime projection is truthful, but exact winner/override chains cover public launcher inputs only; internal fields not yet migrated to the owned resolver do not claim invented origins. Explanation generates no durable state but legacy mode may create an in-memory ephemeral token so the planned fingerprint truthfully records `authToken=set`; the value is never serialized or printed. Roll back the STEP-524 explanation module/CLI/help/docs/tests plus the bounded smoke/fingerprint-parity repairs while retaining STEP-519–523, then append any historical correction here. The next bounded configuration slice is read-only `doctor --json` consuming the same snapshot/explanation model. Complete-trace recollection and App refresh remain separate. No staging, commit, push, publication, App refresh, benchmark, credential, OAuth/DNS/Tunnel, service, deployment, or public runtime state changed.

### STEP-525 make doctor diagnostics machine-readable from one resolved configuration

**Status / goal:** Completed under the owner's instruction to execute the next local improvement without another ChatGPT Web benchmark rerun. Add read-only `codexgpt doctor --json` through the supported public entry. Its stdout must remain one parseable document on success and diagnostic blockers, retain the existing nonzero blocker exit, embed the exact secret-redacted `config explain` model rather than reconstructing configuration, and include the public wrapper's Bash, saved-profile, and OAuth outcomes without exposing secret values.

**Files changed / implementation:** Updated `scripts/codexgpt.mjs`, `scripts/doctor.mjs`, `test/doctor-start-config-parity.test.mjs`, `test/doctor-shell.test.mjs`, `README.md`, `README_ZH.md`, `Memory.md`, and this archive. Internal doctor checks are structured `{status,label,detail}` records; JSON schema v1 reports `ok`, failure/warning counts, the exact nested configuration-explanation document, and all checks. Text output remains the default. The public wrapper captures the internal JSON, appends structured Bash/profile validation and a bounded pass/blocker OAuth result, recalculates summary/exit state, and emits no wrapper prose to stdout. Invalid saved enum values are no longer echoed. Guidance/Skill diagnostics now consume `runtimeConfigSnapshot.effective` directly instead of calling `loadConfig` a second time, so `--no-profile` and every fingerprinted input have one source of truth.

**TDD / corrections:** The initial RED added direct and supported-entry contracts and failed because both layers printed prose before JSON. The first GREEN made those contracts pass. Existing doctor-shell coverage then exposed a deeper C4 violation: the Guidance scan independently reloaded the saved profile and could reject an invalid profile even when `--no-profile` selected the resolved snapshot. Removing that second loader fixed the root cause and preserved the text contract. A further RED proved blocker diagnostics still return parseable JSON with `ok=false`, a nonzero exit, and an exact failing `Local port` check. Final coverage proves the nested configuration object is byte-for-JSON equivalent to `config explain --json`, `CODEXGPT_HOME` is not created, configured secrets project only as `set`, and public wrapper checks are present.

**Exact verification:** `npm run build` passed. The broad focused configuration/profile/OAuth/doctor/help/server/package/mutation command passed 117/117. `node scripts/toolchain-manager.mjs matrix --major all --root C:\Users\Administrator\AppData\Local\CodexPro\toolchains -- node --test test/config-explain.test.mjs test/config-fingerprint.test.mjs test/config-resolver.test.mjs test/doctor-start-config-parity.test.mjs test/doctor-shell.test.mjs test/public-cli-help.test.mjs test/workspace-profile-persistence.test.mjs` passed 51/51 on Node 20.20.2 and 51/51 on Node 24.15.0; managed `npm run build` passed on both. `npm run task:run -- -- node scripts/http-smoke-compat.mjs`, `npm run policy:check`, `git diff --check` apart from informational existing LF-to-CRLF warnings, and the scoped secret-pattern scan passed. No ChatGPT Web benchmark or App refresh was run.

**Risks / rollback / next:** JSON check labels/details are versioned only by the enclosing schema and should be treated as diagnostic data, not a stable automation command surface. The public OAuth wrapper intentionally reports only pass versus blockers in JSON so child diagnostic text cannot leak an unreviewed value; the normal text doctor retains detailed OAuth output. Doctor still performs read-only environment/port/tool discovery, so it is not a pure offline bundle. Roll back the STEP-525 doctor/help/test/docs hunks and restore Guidance's prior import only if also accepting its proven `--no-profile` divergence; retain STEP-519–524. The next bounded configuration slice is C5 network/auth migration compatibility: freeze old-input equivalence and structured deprecation/remediation before moving one setting group. Complete-trace recollection and App refresh remain separate. No staging, commit, push, publication, App refresh, benchmark, credential, OAuth/DNS/Tunnel, service, deployment, or public runtime state changed.

### STEP-526 add the first C5 secret-safe compatibility migration contract

**Status / goal:** Completed under the owner's instruction to execute the next plan step without another ChatGPT Web benchmark rerun. Begin C5 with one existing, supported legacy auth input rather than deleting aliases or migrating multiple setting groups together. Preserve the selected `CODEBASE_BRIDGE_HTTP_TOKEN` value, effective runtime projection, public fingerprint, auth defaults, and restart behavior while adding a structured warning and one executable remediation that never contains the token.

**Files changed / implementation:** Updated `scripts/config-explain.mjs`, `scripts/codexgpt.mjs`, `src/configResolver.ts`, `test/config-explain.test.mjs`, `test/config-resolver.test.mjs`, `test/doctor-start-config-parity.test.mjs`, `README.md`, `README_ZH.md`, `Memory.md`, and this archive. Public input explanations now distinguish a selected compatibility origin from canonical environment input. Explanation schema v1 adds a flat `diagnostics` array and per-input diagnostic projection. Selected `CODEBASE_BRIDGE_HTTP_TOKEN` produces `CONFIG_COMPATIBILITY_INPUT` with replacement `CODEXGPT_HTTP_TOKEN`, the migration-window marker, `valueState=set`, and the secret-free PowerShell command `$env:CODEXGPT_HTTP_TOKEN = $env:CODEBASE_BRIDGE_HTTP_TOKEN; Remove-Item Env:CODEBASE_BRIDGE_HTTP_TOKEN`. Text explanation prints the same warning/fix. Doctor promotes it to a warning check and embeds the identical explanation. The shared TypeScript diagnostic contract now also requires structured replacement/remediation; bootstrap root compatibility receives its own value-free command. A canonical token source wins without a compatibility diagnostic, preserving existing precedence.

**TDD / corrections:** Initial RED compared canonical and legacy-token config explanations, required identical runtime/fingerprint, exact structured migration metadata, no secret occurrence, text remediation, and a public doctor warning; both new cases failed because compatibility input was modeled as an ordinary environment source and the document had no diagnostics. The first GREEN introduced one compatibility candidate and document-level aggregation. Review then found the public JS diagnostic would otherwise diverge from the owned TypeScript `ConfigDiagnostic`; a focused resolver assertion made replacement/remediation mandatory in the shared type and generic compatibility path. Existing canonical-shadow behavior remains unchanged: compatibility diagnostics appear only when the compatibility candidate is selected.

**Exact verification:** `npm run build` passed. The broad configuration/profile/OAuth/doctor/help/server/package/mutation command passed 119/119. `node scripts/toolchain-manager.mjs matrix --major all --root C:\Users\Administrator\AppData\Local\CodexPro\toolchains -- node --test test/config-resolver.test.mjs test/config-explain.test.mjs test/doctor-start-config-parity.test.mjs test/doctor-shell.test.mjs test/phase-8-auth-config.test.mjs test/public-cli-help.test.mjs` passed 60/60 on Node 20.20.2 and 60/60 on Node 24.15.0; managed `npm run build` passed on both. `npm run task:run -- -- node scripts/http-smoke-compat.mjs`, `npm run policy:check`, `git diff --check` apart from informational existing LF-to-CRLF warnings, and the scoped secret-pattern scan passed. No ChatGPT Web benchmark or App refresh was run.

**Risks / rollback / next:** The warning is intentionally advisory during the migration window; the compatibility input remains readable and no removal date is claimed. Only the selected legacy static-token variable is covered. Other historical aliases have inconsistent supported-entry/runtime behavior and must not be labeled equivalent until measured explicitly. Roll back the STEP-526 compatibility candidate/diagnostic/docs/tests while retaining STEP-519–525; this restores silent legacy-token acceptance without changing its value. The next bounded C5 slice is a read-only network compatibility inventory/equivalence gate for `CODEBASE_BRIDGE_REPO_ROOT`, `HOST`, `PORT`, and hostname aliases before changing behavior. Complete-trace recollection and App refresh remain separate. No staging, commit, push, publication, App refresh, benchmark, credential, OAuth/DNS/Tunnel, service, deployment, or public runtime state changed.

### STEP-527 reconcile active documentation, project memory, and rules

**Status / goal:** Completed as an end-of-session knowledge cleanup requested by the owner. Reconcile the STEP-516–526 working-tree behavior with the active changelog, FAQ, security guidance, example configuration, and concise project-memory index. Reduce duplicated historical narration before `Memory.md` reaches its practical byte limit, audit the workspace rules and documentation links, and preserve the intentionally dirty implementation tree without changing runtime behavior or external state.

**Files changed / implementation:** Updated `CHANGELOG.md`, `FAQ.md`, `FAQ_ZH.md`, `SECURITY.md`, `config.example.env`, `Memory.md`, and this append-only archive. The unreleased changelog now summarizes the persistent-process and explainable-configuration work. English and Chinese FAQ guidance points to the read-only secret-redacted diagnostics. Security rules and the example environment describe `CODEBASE_BRIDGE_HTTP_TOKEN` only as retained migration compatibility, name the canonical replacement, and avoid printing or duplicating a token. `Memory.md` removes repeated Phase 8, workspace-capability, release, and benchmark detail already retained in authoritative specs and archives while preserving current decisions, limitations, open items, and all archive links. `AGENTS.md` remains unchanged: at 184 lines and 15,078 bytes it is near the cleanup skill's soft size signal, but its contents are active project rules rather than historical narration. No `CLAUDE.md` was created because Windows uses `AGENTS.md` as the explicit project authority and the applicable cleanup rule forbids inventing a Unix symlink requirement.

**Exact verification:** `npm run test:focused -- test/auth-documentation.test.mjs test/public-cli-help.test.mjs test/package-contents.test.mjs` passed 10/10. `npm run policy:check` passed. A read-only audit found all 24 critical rule/reference paths and all local Markdown targets across 147 project Markdown files. The added-line credential-pattern scan passed. Before this entry, `Memory.md` was reduced from 17,779 to 15,603 bytes and remained under its 150-line/18-KB practical limits; this archive was 37,939 bytes and remained below its 48-KB continuation threshold.

**Risks / rollback / next:** This was documentation and memory reconciliation only; build, runtime smoke, managed Node matrix, ChatGPT Web benchmark, and App refresh were not rerun because STEP-527 changes no runtime source. Large historical plans remain unsplit because they are closed evidence/design records, not active user guidance, and splitting them would create broad link churn without user benefit. Roll back only the STEP-527 documentation/index hunks and append a correction rather than rewriting this archive. The next bounded product slice remains the read-only C5 network compatibility inventory/equivalence gate for `CODEBASE_BRIDGE_REPO_ROOT`, `HOST`, `PORT`, and hostname aliases. No staging, commit, push, publication, deletion, rename, project-external memory edit, credential, OAuth/DNS/Tunnel, service, deployment, or public runtime state changed.

### STEP-528 freeze the network configuration compatibility equivalence gate

**Status / goal:** Completed under the owner's instruction to execute the next plan step without another ChatGPT Web benchmark rerun. Measure the supported public-entry and direct-runtime behavior of `CODEBASE_BRIDGE_REPO_ROOT`, generic `HOST`/`PORT`, canonical host/port/root inputs, stable-hostname aliases, and saved hostname selection before issuing any further compatibility warning. Produce a fail-closed characterization gate and a reviewable decision matrix without changing an input's value, precedence, runtime authority, profile, network, or deployment behavior.

**Files changed / implementation:** Added `test/network-config-compatibility.test.mjs` and `docs/reviews/2026-08-28-network-configuration-compatibility-inventory.md`; updated `scripts/test-execution-profile-manifest.mjs`, `Memory.md`, and this archive. The test executes the supported public entry against isolated roots and separately loads the direct resolver. It proves that `CODEBASE_BRIDGE_REPO_ROOT` selects the direct runtime root but is ignored during public root/profile planning; generic `HOST` and `PORT` are direct-runtime fallbacks but the public planner publishes its canonical defaults; `CODEXGPT_HOSTNAME` and `NGROK_DOMAIN` produce the canonical public hostname value/fingerprint but entry-layer promotion collapses their original source to `CODEXGPT_PUBLIC_HOSTNAME`; `TAILSCALE_FUNNEL_HOSTNAME` is setup-only; and `--url` remains a value-equivalent CLI alias with provenance preserved. The gate joins the reviewed Windows fast profile. The review matrix classifies each input and explicitly prevents warnings/removal claims where public equivalence is absent.

**TDD / corrections:** The initial equivalence assumption failed with the exact observed matrix: public compatibility root false, public generic host/port false, direct compatibility root and generic host/port true, `CODEXGPT_HOSTNAME` and `NGROK_DOMAIN` public fingerprint equivalence true, and Tailscale public-start equivalence false. That RED established that the variables cannot be migrated as one group. The first harness used non-loopback `127.0.0.2` together with `--no-auth` and correctly hit the existing auth safety boundary before measurement; the fixture switched to a synthetic token without weakening production behavior. A later expected-object mismatch corrected the test to the actual explanation schema fields `restartRequired` and `diagnostics`.

**Exact verification:** `npm run test:focused -- test/network-config-compatibility.test.mjs` passed 2/2. The final broader command `npm run test:focused -- test/network-config-compatibility.test.mjs test/config-resolver.test.mjs test/config-explain.test.mjs test/doctor-start-config-parity.test.mjs test/cli-hostname-propagation.test.mjs test/test-execution-profiles.test.mjs` passed 45/45. `npm run policy:check` passed.

**Risks / rollback / next:** This gate records current behavior, including known asymmetry; it does not turn a direct-runtime fallback into a supported public alias. Tests that assert non-equivalence must change deliberately with the corresponding repair. Build, runtime Smoke, managed Node matrix, ChatGPT Web benchmark, and App refresh were not run because no runtime source or public input behavior changed. Roll back the STEP-528 test/review/manifest/index hunks and append a correction rather than rewriting this archive. The next bounded slice is public-entry parity for `CODEBASE_BRIDGE_REPO_ROOT` only: use the owned bootstrap resolver before profile selection, retain `--root` then `CODEXGPT_ROOT` precedence, preserve original compatibility provenance, and add one value-free migration warning. Generic `HOST`/`PORT` and hostname aliases remain separate. No staging, commit, push, publication, credential, profile, OAuth/DNS/Tunnel, service, deployment, or public runtime state changed.

### STEP-529 repair legacy-root public-entry parity and migration diagnostics

**Status / goal:** Completed under the owner's instruction to execute the next approved C5 slice without another ChatGPT Web benchmark rerun. Make selected `CODEBASE_BRIDGE_REPO_ROOT` choose the same workspace and saved profile through the supported public entry as the owned direct resolver, retain exact `--root` then `CODEXGPT_ROOT` precedence, preserve the compatibility origin through `config explain` and doctor, and emit one executable PowerShell migration command that does not embed the selected path. Leave generic `HOST`/`PORT`, hostname aliases, root authority, profiles, network behavior, and deployment unchanged.

**Files changed / implementation:** Updated `scripts/codexgpt-entry.mjs`, `scripts/codexgpt.mjs`, `scripts/doctor.mjs`, `test/network-config-compatibility.test.mjs`, `CHANGELOG.md`, `README.md`, `README_ZH.md`, `FAQ.md`, `FAQ_ZH.md`, `config.example.env`, `docs/reviews/2026-08-28-network-configuration-compatibility-inventory.md`, `Memory.md`, and this archive. The public entry's read-only profile hints now use legacy root only after CLI and canonical root are absent. The launcher and internal doctor call the built owned `resolveConfigBootstrap` before loading a profile, so strict duplicate/invalid/no-profile behavior and exact precedence are shared with the runtime. The public doctor wrapper preserves the caller cwd and raw environment when spawning its internal planner instead of injecting a synthetic canonical root, preventing compatibility provenance loss. `workspace.root` explanation now includes the legacy candidate with replacement `--root or CODEXGPT_ROOT` and the value-free remediation `$env:CODEXGPT_ROOT = $env:CODEBASE_BRIDGE_REPO_ROOT; Remove-Item Env:CODEBASE_BRIDGE_REPO_ROOT`. Doctor promotes the same structured diagnostic to a warning. Active user docs and the STEP-528 matrix now describe the repaired boundary.

**TDD / corrections:** The initial RED had two failures: the public plan still selected caller cwd instead of the compatibility target, and saved-profile selection followed that wrong root. After routing the launcher through bootstrap resolution and teaching entry/doctor wrappers the compatibility fallback, the parity and diagnostic contracts passed. The saved-profile start prints the established setup summary before `--print-env-only`; the test initially parsed stdout as pure JSON, then correctly selected the JSON document without changing production output. Final coverage proves the target profile port is selected, the entry's hostname/Tunnel hints use the same target profile, the compatibility diagnostic contains variable names but not the selected path, doctor reports the same warning, and CLI/canonical winners do not warn.

**Exact verification:** `npm run build` passed. `npm run test:focused -- test/network-config-compatibility.test.mjs test/config-resolver.test.mjs test/config-explain.test.mjs test/config-fingerprint.test.mjs test/doctor-start-config-parity.test.mjs test/doctor-shell.test.mjs test/cli-hostname-propagation.test.mjs test/cloudflared-installer.test.mjs test/public-cli-help.test.mjs test/workspace-profile-schema.test.mjs test/workspace-profile-persistence.test.mjs test/test-execution-profiles.test.mjs test/package-contents.test.mjs` passed 82/82. The retained managed Node 20.20.2 and 24.15.0 matrix passed 34/34 on each major across network compatibility, explanation, doctor, hostname, and verified-entry tests; managed `npm run build` passed on both. `npm run task:run -- -- node scripts/http-smoke-compat.mjs` passed. `npm run policy:check` passed.

**Risks / rollback / next:** The entry's profile-hint layer performs only read-only root selection before the owned launcher resolver performs strict validation; invalid or duplicate selected inputs still fail before profile mutation/server start. Normal start output does not add a new warning; `config explain` and doctor are the structured migration surfaces, matching the prior token slice. Roll back the STEP-529 entry/launcher/doctor/test/docs hunks while retaining STEP-528; this restores the measured public/direct asymmetry without changing the direct resolver. The next bounded slice is preserving original `CODEXGPT_HOSTNAME` provenance, keeping its runtime/fingerprint equivalence, and adding a value-free warning only when it wins. Generic `HOST`/`PORT`, `NGROK_DOMAIN`, and Tailscale setup remain separate. No staging, commit, push, publication, credential, profile mutation outside test fixtures, OAuth/DNS/Tunnel, service, deployment, App refresh, benchmark, or public runtime state changed.

Part 11 is closed after STEP-529 because it reached the 48 KB continuation threshold. Start STEP-530 in `interphase-maintenance-part-12.md`; do not append further entries here.
